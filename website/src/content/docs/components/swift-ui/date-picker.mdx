A controlled native date or date-and-time picker.

## Import

Import `DatePicker` from `@quickgui/ui/swift-ui`.

```tsx
import { Host, DatePicker } from "@quickgui/ui/swift-ui";
```

## Usage

```tsx
const [scheduledAt, setScheduledAt] = createSignal(Date.now());

<Host matchContents>
  <DatePicker
    label="Schedule"
    value={scheduledAt()}
    displayedComponents="dateAndTime"
    style="field"
    onValueChange={setScheduledAt}
  />
</Host>
```

## Key props

| Prop | Purpose |
| --- | --- |
| `value` | Unix timestamp in milliseconds; `min` and `max` use the same unit. |
| `min` | Minimum accepted value. |
| `max` | Maximum accepted value. |
| `displayedComponents` | Chooses date, time, or combined date-and-time fields. |
| `style` | `automatic`, `field`, `stepperField`, or `graphical`. |
| `onValueChange` | Reports the value accepted by the core. |

