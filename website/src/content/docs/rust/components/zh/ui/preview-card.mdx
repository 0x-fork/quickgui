在触发器旁显示更丰富的悬停或聚焦预览，不改变当前页面。

## 导入

从 `quickgui` 导入 `PreviewCard`。

```rust
use quickgui::*;
```

## 用法

```rust
let card = PreviewCard::new("file", &state);
card.trigger_with(text("README.md"))
```

## 结构

- `PreviewCard::root`
- `PreviewCard::trigger`
- `PreviewCard::portal`
- `PreviewCard::backdrop`
- `PreviewCard::positioner`
- `PreviewCard::popup`
- `PreviewCard::arrow`

## 主要属性

| 属性 | 说明 |
| --- | --- |
| `open` | 受控的打开状态。 |
| `default_open` | 组件自行管理打开状态时的初始状态。 |
| `delay` | 打开前由核心管理的延迟（毫秒）。 |
| `close_delay` | 关闭前由核心管理的延迟（毫秒）。 |
| `placement` | 碰撞处理前的首选放置方向。 |
