サブメニュー、グループ、リンク、チェック項目、ラジオ項目を組み合わせられるメニューです。

## インポート

`quickgui` から `PopoverMenu` をインポートします。

```rust
use quickgui::*;
```

## 使い方

```rust
let menu = PopoverMenu::new("actions", &state);
menu.root().child(menu.trigger().child("Actions"))
```

## 構成

- `PopoverMenu::root`
- `PopoverMenu::trigger`
- `PopoverMenu::portal`
- `PopoverMenu::backdrop`
- `PopoverMenu::positioner`
- `PopoverMenu::popup`
- `PopoverMenu::arrow`
- `PopoverMenu::item`
- `PopoverMenu::link_item`
- `PopoverMenu::submenu_root`
- `PopoverMenu::submenu_trigger`
- `PopoverMenu::group`
- `PopoverMenu::group_label`
- `PopoverMenu::radio_group`
- `PopoverMenu::radio_item`
- `PopoverMenu::radio_item_indicator`
- `PopoverMenu::checkbox_item`
- `PopoverMenu::checkbox_item_indicator`
- `PopoverMenu::separator`

## 主なプロパティ

| プロパティ | 説明 |
| --- | --- |
| `open` | 制御された開閉状態です。 |
| `default_open` | コンポーネント自身が開閉状態を管理するときの初期状態です。 |
| `on_open_change` | コアが決定した開閉を通知します。 |
| `orientation` | 水平または垂直のレイアウトと、それに対応するキーボード方向です。 |
| `loop_focus` | 現在のメニューレベルの端でキーボードハイライトを循環させます。 |
