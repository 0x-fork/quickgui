QuickGUI アプリは通常の Go プログラムです。Go でコンポーネントと細粒度のリアクティブな状態を記述し、`CGO_ENABLED=0` でビルドします。purego が同じプロセス内でビルド済みネイティブライブラリを読み込み、Bun は開発・パッケージング用 CLI を実行します。

## 動作要件

- macOS 14 以降
- Go 1.23 以降
- Bun 1.3 以降
- Xcode Command Line Tools（`xcode-select --install`）

現在、パッケージ化した UI アプリは macOS が対象です。通常のアプリビルドではビルド済みのネイティブホストを使用します。

## プロジェクトを作成

プロジェクトを作成し、ネイティブ開発アプリを起動します。

```bash
bunx @quickgui/cli init my-app
cd my-app
bun run dev
```

## 最初のウィンドウ

生成された `main.go` は、ウィンドウ API を `native`、コンポーネントを `ui` からインポートします。コンポーネント関数を `WindowOptions.Component` に直接渡し、`native.Run` を一度呼んでイベントループを開始します。

```go
package main

import (
	"github.com/egoist/quickgui/go/native"
	"github.com/egoist/quickgui/go/ui"
	"log"
)

func Counter() {
	count, setCount := ui.CreateSignal(0)
	ui.View(
		func() {
			ui.Text("Count: ", count)
			ui.Button(
				"Increment",
				ui.Style{Padding: 12, BorderRadius: 8, BackgroundColor: "#2563eb", Color: "white"},
				ui.OnClick(func() { setCount(count() + 1) }),
			)
		},
		ui.Style{
			Display:        "flex",
			FlexDirection:  "column",
			Width:          "100%",
			Height:         "100%",
			AlignItems:     "center",
			JustifyContent: "center",
			Gap:            12,
		},
	)
}

func main() {
	if err := native.Run(func() {
		open := func() {
			native.NewWindow(native.WindowOptions{
				Title:     "My App",
				Width:     720,
				Height:    480,
				Component: Counter,
			})
		}
		native.App.OnReopen(func(event native.ReopenEvent) {
			if !event.HasVisibleWindows {
				open()
			}
		})
		open()
	}); err != nil {
		log.Fatal(err)
	}
}
```

## 実行とビルド

ウォッチャーは Go ソースを再ビルドして開発アプリを再起動します。フォーマッターは通常の Go 構文を保ちながら、長いコンポーネント呼び出しの改行を整えます。配布用バンドルは `dist/darwin-*` に出力されます。

```bash
bun run dev
bun run fmt
bun run build
```

## 次のステップ

- [コンポーネントとシグナル](/docs/ui)を理解する。
- [スタイルと条件付きオプション](/docs/styling)を追加する。
- [コンポーネントリファレンス](/docs/components)を確認する。
