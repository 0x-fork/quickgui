Presents timed, stacked notifications with action, close, and swipe behavior.

## Import

Add these package imports to `moon.pkg`:

```moonbit
import {
  "egoist/quickgui/ui",
  "egoist/quickgui/reactive",
}
```

## Usage

```moonbit
fn component_example() -> @ui.Element {
  let viewport = @ui.toast_viewport([])
    .toasts([{ "id": "saved", "title": "Saved", "timeout": 5000 }])
    .timeout(5000)
  let toast = viewport.slot(@ui.Item, value="saved")
  viewport.children([
    toast
    .padding(16)
    .children([
      toast.slot(@ui.Title).label("Saved"),
      toast.slot(@ui.Close).label("Dismiss"),
    ]),
  ])
}
```

The native core owns interaction, layout, accessibility and animation. These parts are unstyled: set sizes, colors and spacing for your application.

## Anatomy

Create parts from the root or an item with `.slot(...)`. They inherit the component scope and item identity. Each call returns an `Element`, so the same fluent styling and child methods work throughout.

- `root.slot(@ui.Portal)`
- `root.slot(@ui.Positioner)`
- `root.slot(@ui.Content)`
- `root.slot(@ui.Title)`
- `root.slot(@ui.Description)`
- `root.slot(@ui.Action)`
- `root.slot(@ui.Close)`
- `root.slot(@ui.Item)`

## Key props

Read signal getters directly in fluent property setters. The QuickGUI CLI creates controlled bindings for those expressions. Use change callbacks to write state; literal properties initialize component-owned state. Explicit `bind_` methods remain available for lower-level composition.

| Method | Purpose |
| --- | --- |
| `toasts(...)` | Complete declared toast queue rendered by the viewport. |
| `timeout(...)` | Inherited auto-dismiss duration or warm-provider timeout. |
| `limit(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Limit) |
| `swipe_direction(...)` | Edge toward which a swipe dismisses or moves the surface. |
| `stack_expanded(...)` | Native component configuration; `bind_` accepts a reactive accessor. (StackExpanded) |
| `on_change(handler)` | Receives semantic component state reported by the native core. |
| `style(...), [children]` | Layout, typography, colors, interaction states and child composition. |
