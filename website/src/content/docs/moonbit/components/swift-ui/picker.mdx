A controlled SwiftUI picker with menu, segmented, radio-group, and inline styles.

## Import

Add these package imports to `moon.pkg`:

```moonbit
import {
  "egoist/quickgui/ui",
}
```

## Usage

```moonbit
fn component_example() -> @ui.Element {
  let (value, set_value) = @ui.create_signal("go")
  @ui.swift_ui_host([
    @ui.swift_ui_picker()
    .value(value())
    .on_input(next => set_value(next))
    .items([
      { "value": "go", "label": "Go" },
      { "value": "moonbit", "label": "MoonBit" },
    ])
    .label("Language"),
  ])
}
```

SwiftUI controls require macOS and a `swift_ui_host([])` ancestor. Use `label(...)` for a native control label, `value(...)` for its value, and `swift_ui_modifiers(...)` for native SwiftUI modifiers.

## Key props

SwiftUI controls require macOS and a `swift_ui_host([])` ancestor. Use `label(...)` for a native control label, `value(...)` for its value, and `swift_ui_modifiers(...)` for native SwiftUI modifiers.

| Method | Purpose |
| --- | --- |
| `signal(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Signal) |
| `swift_ui_host(...)` | Native component configuration; `bind_` accepts a reactive accessor. (SwiftUiHost) |
| `swift_ui_picker(...)` | Native component configuration; `bind_` accepts a reactive accessor. (SwiftUiPicker) |
| `bind_value(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Value) |
| `get(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Get) |
| `on_input(...)` | Native component configuration; `bind_` accepts a reactive accessor. (OnInput) |
| `set(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Set) |
| `items(...)` | Native component configuration; `bind_` accepts a reactive accessor. (Items) |
| `label(...)` | Visible native SwiftUI control label. |
| `style(...), [children]` | Layout, typography, colors, interaction states and child composition. |
