Filters suggestions for free-form text without requiring a selected value.

## Import

Import `ui` from `github.com/egoist/quickgui/go/ui`.

```go
import (
	"fmt"
	"github.com/egoist/quickgui/go/native"
	"github.com/egoist/quickgui/go/ui"
)
```

## Usage

```go
func AutocompleteExample() *native.Node {
	autocomplete1 := ui.NewAutocomplete(ui.AutocompleteRootProps{PickerInputProps: ui.PickerInputProps{PickerSourceProps: ui.PickerSourceProps{Items: func() []ui.OptionDeclaration {
		return []ui.OptionDeclaration{{Value: "go", Label: "Go"}, {Value: "swift", Label: "Swift"}}
	}}, Placeholder: "Find a language", OnInputValueChange: func(query string, _ *native.Event) {
		fmt.Println(query)
	}}})
	return autocomplete1.Root().NativeNode()
}
```

## Anatomy

- `autocomplete.Root`
- `autocomplete.Input`
- `autocomplete.Option`
- `autocomplete.Label`
- `autocomplete.Value`
- `autocomplete.Icon`
- `autocomplete.InputGroup`
- `autocomplete.Clear`
- `autocomplete.Portal`
- `autocomplete.Positioner`
- `autocomplete.Popup`
- `autocomplete.Arrow`
- `autocomplete.Status`
- `autocomplete.Empty`
- `autocomplete.List`
- `autocomplete.Item`
- `autocomplete.ItemIndicator`
- `autocomplete.Group`
- `autocomplete.GroupLabel`
- `autocomplete.Collection`
- `autocomplete.Separator`

## Key props

| Prop | Purpose |
| --- | --- |
| `Items` | Bounded declaration model used for options or keyboard navigation. |
| `InputValue` | Editable query text used by a combobox or autocomplete. |
| `FilterMode` | Core-owned contains, prefix, fuzzy, or disabled filtering. |
| `OnInputValueChange` | Reports the editable query text retained by the core. |
| `OnCommit` | Reports one explicit commit boundary and its final value details. |
