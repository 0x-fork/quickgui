Shows a richer hover or focus preview beside a trigger without changing the current screen.

## Import

Import `ui` from `github.com/egoist/quickgui/go/ui`.

```go
import (
	"github.com/egoist/quickgui/go/ui"
)
```

## Usage

```go
func PreviewCardExample() *ui.Element {
	card := ui.NewPreviewCard().Placement("bottom-start")
	return card.Root().Children(
		card.Trigger(ui.PreviewCardTriggerProps{Delay: 350}).Child("@ada"),
		card.Positioner().Child(
			card.Popup().Child("Ada Lovelace"),
		),
	)
}
```

## Anatomy

- `previewCard.Root`
- `previewCard.Trigger`
- `previewCard.Portal`
- `previewCard.Backdrop`
- `previewCard.Positioner`
- `previewCard.Popup`
- `previewCard.Arrow`

## Key props

| Prop | Purpose |
| --- | --- |
| `Open` | Controlled open state. |
| `DefaultOpen` | Initial state when the component owns whether it is open. |
| `Delay` | Core-owned delay in milliseconds before opening. |
| `CloseDelay` | Core-owned delay in milliseconds before closing. |
| `Placement` | Preferred placement side before collision handling. |
