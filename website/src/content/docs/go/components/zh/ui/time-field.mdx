可通过键盘编辑的分段时间字段，包含时、分、秒和上下午部分。

## 导入

从 `github.com/egoist/quickgui/go/ui` 导入 `ui`。

```go
import (
	"github.com/egoist/quickgui/go/ui"
)
```

## 用法

```go
func TimeFieldExample() *native.Node {
	return ui.TimeField.Root(
		ui.TimeFieldRootProps{DefaultValue: "09:30", Hour12: true},
		func() *native.Node {
			var children []*native.Node
			for _, segment := range []string{"hour", "minute", "period"} {
				children = append(children, ui.TimeField.Segment(ui.TimeFieldSegmentProps{Segment: segment}))
			}
			return ui.Fragment(children)
		},
	)
}
```

## 结构

- `ui.TimeField.Root`
- `ui.TimeField.Segment`

## 主要属性

| 属性 | 说明 |
| --- | --- |
| `Value` | 受控值，具体类型取决于该组件的约定。 |
| `DefaultValue` | 组件自行管理状态时的初始值。 |
| `Hour12` | 添加上午/下午分段，并使用 12 小时制。 |
| `ShowSeconds` | 挂载秒数分段。 |
| `OnValueChange` | 报告核心接受的值。 |
