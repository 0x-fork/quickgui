QuickGUI 将无样式基础组件与具备无障碍行为的复合控件组合在一起。两者都可以通过普通 Go 函数进行组合。

## 定义组件

Go 组件是声明原生子节点的 `func()`。用普通参数配置组件，需要持续更新的值则传入信号访问器。每次调用都可以拥有自己的状态和清理逻辑。

```go
func CounterLabel(count func() int) {
	ui.Text("Count: ", count).FontSize(20)
}

func ComponentExample() {
	count, setCount := ui.CreateSignal(0)
	ui.View(func() {
		CounterLabel(count)
		ui.Button("Increment", ui.OnClick(func() { setCount(count() + 1) }))
	})
}
```

用回调传递操作，用 children 回调编写可复用容器。在容器内部调用 children，让节点挂到正确的父节点下。普通 Go 函数调用共享当前挂载作用域；当条件分支、带键行或窗口根等所属作用域销毁时，相应清理会执行。

```go
func CardComponent(title string, children ui.Component) {
	ui.View(
		func() {
			ui.Text(title).FontWeight(600)
			children()
		},
	).Padding(20).Gap(12).Display("flex").FlexDirection("column")
}

func CardComposition() {
	CardComponent("Account", func() {
		ui.Text("Manage your account settings.")
		ui.Button("Save", ui.OnClick(func() { log.Print("Saved") }))
	})
}
```

## 基础组件

从 `ui.View`、`ui.Text`、`ui.Button` 和 `ui.Input` 开始。可以直接传入选项与文本。`func() { ... }` 参数将多个子节点分组到当前父节点下。

```go
func ButtonExample(save func()) {
	ui.Button(
		"Save changes",
		ui.Style().Height(40),
		ui.Style().PaddingLeft(16),
		ui.Style().PaddingRight(16),
		ui.Style().BackgroundColor("#2563eb"),
		ui.Style().TextColor("white"),
		ui.Style().BorderRadius(8),
		ui.Style().Hover(func(s ui.StyleBuilder) ui.StyleBuilder { return s.BackgroundColor("#3b82f6") }),
		ui.OnClick(save),
	)
}
```

## 复合组件

复合组件族暴露 `ui.Tabs.Root`、`ui.Tabs.List`、`ui.Tabs.Panel` 等部件。核心负责角色、键盘导航、焦点与控件状态。请在根节点的子节点回调中创建部件，以确保上下文已经存在。

```go
func TabsExample() {
	ui.Tabs.Root(
		ui.TabsRootProps{DefaultValue: "overview", Activation: "automatic"},
		func() {
			ui.Tabs.List(
				ui.PartProps{},
				func() {
					ui.Tabs.Tab(ui.TabsTabProps{Value: "overview"}, "Overview")
					ui.Tabs.Tab(ui.TabsTabProps{Value: "settings"}, "Settings")
				},
			)
			ui.Tabs.Panel(ui.TabsPanelProps{Value: "overview"}, "Overview content")
			ui.Tabs.Panel(ui.TabsPanelProps{Value: "settings"}, "Settings content")
		},
	)
}
```

## 受控状态

受控值使用访问器，其回调保存控件接受的新值。使用默认值可让组件自己管理状态。可选标量使用指针，多选值使用切片。

```go
func CheckboxExample() {
	checked, setChecked := ui.CreateSignal(false)
	ui.Checkbox.Root(
		ui.CheckboxProps{
			Checked:         func() ui.CheckedState { return checked() },
			OnCheckedChange: func(next bool, _ *native.Event) { setChecked(next) },
		},
		func() { ui.Checkbox.Indicator(ui.PartProps{}, "✓"); ui.Text("Remember me") },
	)
}
```

## 组件类别

参考文档覆盖表单、选择器、布局与数据、浮层、菜单和路由。`examples/components` 包含原始画廊的全部 42 个演示，包括 5,000 行表格和懒加载树。独立的 `examples/swift-ui` 画廊演示原生 SwiftUI 托管。

```bash
bun packages/cli/src/cli.ts dev --project examples/components
```

## 设置各部件的样式

通过 `PartProps.Style` 设置部件样式。使用 `ui.Style()` 和链式方法构建样式，通过 `.Merge(...)` 合并共享构建器。部件也接受构建器切片及返回这些值的访问器。选中状态和主题信号会更新保留的部件，不必重建子节点。

```go
ui.PartProps{
	Style: ui.Style().
		Display("flex").
		Height(32).
		PaddingLeft(12).
		PaddingRight(12).
		BackgroundColor("#2563eb").
		TextColor("white").
		BorderRadius(8),
}
```

组件侧栏中每个原生控件都有独立页面。完整使用流程见[表单与输入](/docs/go/forms-and-input)和[浮层与对话框](/docs/go/overlays-and-dialogs)。
