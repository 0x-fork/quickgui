QuickGUI はスタイルなしのプリミティブとアクセシブルな複合コントロールを組み合わせます。どちらも通常の Go 関数で構成できます。

## コンポーネントの定義

Go コンポーネントはネイティブの子を宣言する `func()` です。通常の引数で設定し、変化する値にはシグナルのアクセサーを渡します。呼び出しごとに状態と後始末を持てます。

```go
func CounterLabel(count func() int) {
	ui.Text("Count: ", count, ui.FontSize(20))
}

func ComponentExample() {
	count, setCount := ui.CreateSignal(0)
	ui.View(func() {
		CounterLabel(count)
		ui.Button("Increment", ui.OnClick(func() { setCount(count() + 1) }))
	})
}
```

操作はコールバックで渡し、再利用するコンテナーには children コールバックを渡します。コンテナーの内側で呼び出すと、子ノードが正しい親に接続されます。通常の Go 関数呼び出しは現在のマウント所有者を共有します。条件分岐、キー付き行、ウィンドウのルートなど、その所有者が破棄されるとクリーンアップが実行されます。

```go
func CardComponent(title string, children ui.Component) {
	ui.View(
		func() {
			ui.Text(title, ui.FontWeight(600))
			children()
		},
		ui.Padding(20),
		ui.Gap(12),
		ui.Display("flex"),
		ui.FlexDirection("column"),
	)
}

func CardComposition() {
	CardComponent("Account", func() {
		ui.Text("Manage your account settings.")
		ui.Button("Save", ui.OnClick(func() { log.Print("Saved") }))
	})
}
```

## プリミティブ

`ui.View`、`ui.Text`、`ui.Button`、`ui.Input` から始めます。オプションとテキストを直接渡せます。`func() { ... }` 引数は複数の子を現在の親の下にまとめます。

```go
func ButtonExample(save func()) {
	ui.Button(
		"Save changes",
		ui.Height(40),
		ui.PaddingLeft(16),
		ui.PaddingRight(16),
		ui.BackgroundColor("#2563eb"),
		ui.TextColor("white"),
		ui.BorderRadius(8),
		ui.Hover(ui.BackgroundColor("#3b82f6")),
		ui.OnClick(save),
	)
}
```

## 複合コンポーネント

複合コンポーネントは `ui.Tabs.Root`、`ui.Tabs.List`、`ui.Tabs.Panel` などのパーツを公開します。コアがロール、キーボード操作、フォーカス、状態を処理します。コンテキストが存在するように、パーツはルートの子要素コールバック内で作成してください。

```go
func TabsExample() {
	ui.Tabs.Root(
		ui.TabsRootProps{DefaultValue: "overview", Activation: "automatic"},
		func() {
			ui.Tabs.List(
				ui.PartProps{},
				func() {
					ui.Tabs.Tab(ui.TabsTabProps{Value: "overview"}, "Overview")
					ui.Tabs.Tab(ui.TabsTabProps{Value: "settings"}, "Settings")
				},
			)
			ui.Tabs.Panel(ui.TabsPanelProps{Value: "overview"}, "Overview content")
			ui.Tabs.Panel(ui.TabsPanelProps{Value: "settings"}, "Settings content")
		},
	)
}
```

## 制御された状態

制御値にはアクセサーを渡し、コールバックでコントロールが受け入れた値を保存します。デフォルト値を使うとコンポーネントが状態を管理します。省略可能なスカラー値はポインター、複数選択はスライスで表します。

```go
func CheckboxExample() {
	checked, setChecked := ui.CreateSignal(false)
	ui.Checkbox.Root(
		ui.CheckboxProps{
			Checked:         func() ui.CheckedState { return checked() },
			OnCheckedChange: func(next bool, _ *native.Event) { setChecked(next) },
		},
		func() { ui.Checkbox.Indicator(ui.PartProps{}, "✓"); ui.Text("Remember me") },
	)
}
```

## コンポーネントの種類

リファレンスにはフォーム、ピッカー、レイアウトとデータ、オーバーレイ、メニュー、ルーティングを収録しています。`examples/components` には5,000行のテーブルと遅延読み込みツリーを含む、元の42デモすべてがあります。`examples/swift-ui` はネイティブ SwiftUI ホスティングの別ギャラリーです。

```bash
bun packages/cli/src/cli.ts dev --project examples/components
```

## 各パーツのスタイル

各パーツのスタイルには `PartProps.Style` を使います。単一のスタイルオプション、または `ui.Styles(...)` で組み合わせた複数のオプションを渡せます。再利用可能なスタイル、そのスライス、それらを返すアクセサーも使えます。選択状態やテーマのシグナルで、子を作り直さず保持されたパーツを更新できます。

```go
ui.PartProps{
	Style: ui.Styles(
		ui.Display("flex"),
		ui.Height(32),
		ui.PaddingLeft(12),
		ui.PaddingRight(12),
		ui.BackgroundColor("#2563eb"),
		ui.TextColor("white"),
		ui.BorderRadius(8),
	),
}
```

Components サイドバーでは各ネイティブコントロールに専用ページがあります。実際の組み合わせは[フォームと入力](/docs/go/forms-and-input)と[オーバーレイとダイアログ](/docs/go/overlays-and-dialogs)を参照してください。
