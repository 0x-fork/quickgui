TypeScript ガイド本文は現在英語です。コンポーネントの API リファレンスには日本語の説明があります。

## スタイルメソッド

Put every layout, typography, and paint declaration in `style`, using camelCase keys. Numeric lengths are logical pixels; supported percentages and keywords are parsed by Rust. `color` and `textColor` declare inherited text color.

```tsx
<View style={{ padding: 16, gap: 12, bg: "#18181b", color: "#fafafa" }} />
```

The 272 fixed layout and style presets are boolean entries in `style`:

```tsx
<View style={{ flexCol: true, p3: true, gap2: true, roundedLg: true }}>
  <Text style={{ textLg: true, fontSemibold: true }}>Panel title</Text>
</View>
```

Preset entries include `roundedLg`, `flexCol`, `itemsCenter`, `p3`, `textLg`, and `fontBold`. `roundedLg` uses an 8-pixel radius; `p3` uses 12 pixels from the four-pixel spacing scale. Custom values use fields such as `borderRadius`, `padding`, `gridTemplateColumns`, and `width`. `positionSticky` is the sticky-layout preset.

Presets expand before style arrays merge. `false`, `null`, or `undefined` withdraws a preset and reveals the underlying style. The preset types and values are generated from Rust by `scripts/generate-style-helpers.ts`.

## スタイルのマージ

Nested style arrays merge left to right; later fields override earlier ones. Falsy entries are ignored. Removing a style field withdraws that declaration.

```tsx
<View style={[base, selected() && selectedStyle, { padding: 20 }]} />
```

## スタイルの再利用

Use `JSX.Style` for reusable styles. Keep getters in JSX when their values should update reactively.

```tsx
import type { JSX } from "@quickgui/solid";

const panel = { p3: true, roundedXl: true, bg: "#20242c" } satisfies JSX.Style;
<View style={panel}><Text>Reusable panel</Text></View>
```

## Flexbox

Use `display`, `flexDirection`, `flexGrow`, `flexShrink`, `flexBasis`, alignment, and gap. Set `minWidth: 0` or `minHeight: 0` on flexible scroll containers so content can shrink into the available viewport.

## グリッド

Use `display: "grid"`, track declarations, placement, and spans for two-dimensional layouts. Grid sizing and intrinsic measurements are native; children keep their identities when positions change.

## テキストと色

Font family, weight, size, line height, wrapping, ellipsis, decoration, and selection use retained native text. Colors accept hex, supported CSS color strings, or packed RGBA values. Images and SVGs share the core’s bounded caches.

## インタラクション状態

```tsx
<Button style={{
  bg: "#2563eb",
  hover: { bg: "#1d4ed8" },
  active: { opacity: 0.8 },
  focusVisible: { outlineColor: "#93c5fd", outlineWidth: 2 },
}}>Save</Button>
```

## グループと名前付きグループのホバー

Use a named `group` on an ancestor and `groupHover` or `groupActive` on descendants to style coordinated states. The core resolves group membership and invalidates the affected retained subtree.
