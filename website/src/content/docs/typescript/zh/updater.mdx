安装 `@quickgui/extension-updater` 即可包含自动更新。未安装的应用不会附带更新器。

## 配置

```sh
bun add @quickgui/extension-updater
```

生成签名密钥对，并把公钥复制到项目中：

```sh
bunx @quickgui/cli keygen --sparkle --out-dir "$HOME/.config/my-app/update-keys"
cp "$HOME/.config/my-app/update-keys/quickgui-update.pub" ./update-public-key.pub
```

不要把私钥 `quickgui-update.key` 提交到版本控制。

```ts
import { readFileSync } from "node:fs";
import { defineConfig } from "@quickgui/cli";

export default defineConfig({
  language: "typescript",
  name: "My App",
  identifier: "com.example.my-app",
  version: "1.0.0",
  entry: "app.tsx",
  extensions: ["@quickgui/extension-updater"],
  updates: {
    baseUrl: "https://downloads.example.com/my-app",
    publicKey: readFileSync(
      new URL("./update-public-key.pub", import.meta.url),
      "utf8",
    ).trim(),
    automaticChecks: true,
  },
});
```

`darwin-arm64` 构建会检查 `https://downloads.example.com/my-app/appcast-darwin-arm64.xml`，除非你设置了 `updates.feedUrl`。该 URL 中的 `{target}` 会替换为当前目标。

## 应用用法

在 `app.whenReady()` 之后、组件之外启动，让每个窗口共用一个会话：

```ts
import { app } from "@quickgui/native";
import Updater from "@quickgui/extension-updater";

await app.whenReady();

export const updater = new Updater({}, event => {
  if (event.error) console.error(event.error);
  if (event.quitRequired) {
    void app.quit().catch(console.error);
  }
});

await updater.ready;
```

```tsx
import { Button } from "@quickgui/solid";
import { updater } from "./updates";

export function CheckForUpdatesButton() {
  return (
    <Button style={{ p3: true, roundedLg: true }} onClick={() => void updater.check()}>
      Check for Updates
    </Button>
  );
}
```

## 生命周期

| API | 用法 |
| --- | --- |
| `updater.check()` | 手动检查更新。 |
| `updater.install()` | 安装可用的更新。 |
| `updater.setAutomaticChecks(enabled)` | 更改自动检查偏好。 |
| `updater.state` | 读取最近一次事件。 |
| `updater.close()` | 停止应用不再需要的会话。 |

状态为 `idle`、`checking`、`available`、`downloading`、`installing` 或 `disabled`。关闭窗口不得关闭共享的更新器。

开发构建默认报告 `disabled`。只在测试 feed 和可丢弃的应用包上使用 `{ allowDevelopment: true }`。

## 平台支持

macOS 使用 Sparkle。Windows 使用已签名的 NSIS 安装程序。Linux 使用 AppImage。通过 Linux 包管理器安装的会报告 `disabled`。当前预构建包覆盖 macOS；其他目标需要在该主机上做原生构建。

## 发布更新

提高 `quickgui.config.ts` 中的 `version`。把私钥加载到环境中：

```sh
export QUICKGUI_UPDATER_PRIVATE_KEY="$(cat "$HOME/.config/my-app/update-keys/quickgui-update.key")"
```

```sh
bunx @quickgui/cli build --target darwin-arm64 --update-manifest \
  --sign "Developer ID Application: Example (TEAMID)" \
  --notarize my-app-notary
```

先上传带版本的归档，再上传 appcast。每种架构有自己的 feed。`updates.manifest: true` 会在每次生产构建时生成 feed。

## 原生产物

在本仓库中，用 `bun packages/native/build.ts --extension updater` 构建一次更新器。打包应用在配置中列出该扩展时会包含它。
