import { defineConfig } from "@quickgui/cli";

export default defineConfig({
  language: "typescript",
  name: "Code Morph",
  identifier: "com.quickgui.code-morph.typescript",
  entry: "app.tsx",
});
