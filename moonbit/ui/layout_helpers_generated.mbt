// Generated by bun scripts/generate-style-helpers.ts. Do not edit.

///|
/// Matches Rust's absolute helper.
pub fn Element::absolute(self : Element) -> Element {
  self.property(@protocol.POSITION, IntoValue::into_value("absolute"))
}

///|
/// Matches Rust's absolute helper.
pub fn Style::absolute(self : Style) -> Style {
  self.property(
    @protocol.POSITION,
    "position",
    IntoValue::into_value("absolute"),
  )
}

///|
/// Matches Rust's app_region_drag helper.
pub fn Element::app_region_drag(self : Element) -> Element {
  self.property(@protocol.APP_REGION, IntoValue::into_value("drag"))
}

///|
/// Matches Rust's app_region_drag helper.
pub fn Style::app_region_drag(self : Style) -> Style {
  self.property(
    @protocol.APP_REGION,
    "appRegion",
    IntoValue::into_value("drag"),
  )
}

///|
/// Matches Rust's app_region_no_drag helper.
pub fn Element::app_region_no_drag(self : Element) -> Element {
  self.property(@protocol.APP_REGION, IntoValue::into_value("no-drag"))
}

///|
/// Matches Rust's app_region_no_drag helper.
pub fn Style::app_region_no_drag(self : Style) -> Style {
  self.property(
    @protocol.APP_REGION,
    "appRegion",
    IntoValue::into_value("no-drag"),
  )
}

///|
/// Matches Rust's aspect_square helper.
pub fn Element::aspect_square(self : Element) -> Element {
  self.property(@protocol.ASPECT_RATIO, IntoValue::into_value(1))
}

///|
/// Matches Rust's aspect_square helper.
pub fn Style::aspect_square(self : Style) -> Style {
  self.property(@protocol.ASPECT_RATIO, "aspectRatio", IntoValue::into_value(1))
}

///|
/// Matches Rust's block helper.
pub fn Element::block(self : Element) -> Element {
  self.property(@protocol.DISPLAY, IntoValue::into_value("block"))
}

///|
/// Matches Rust's block helper.
pub fn Style::block(self : Style) -> Style {
  self.property(@protocol.DISPLAY, "display", IntoValue::into_value("block"))
}

///|
/// Matches Rust's border_dashed helper.
pub fn Element::border_dashed(self : Element) -> Element {
  self.property(@protocol.BORDER_STYLE, IntoValue::into_value("dashed"))
}

///|
/// Matches Rust's border_dashed helper.
pub fn Style::border_dashed(self : Style) -> Style {
  self.property(
    @protocol.BORDER_STYLE,
    "borderStyle",
    IntoValue::into_value("dashed"),
  )
}

///|
/// Matches Rust's border_dotted helper.
pub fn Element::border_dotted(self : Element) -> Element {
  self.property(@protocol.BORDER_STYLE, IntoValue::into_value("dotted"))
}

///|
/// Matches Rust's border_dotted helper.
pub fn Style::border_dotted(self : Style) -> Style {
  self.property(
    @protocol.BORDER_STYLE,
    "borderStyle",
    IntoValue::into_value("dotted"),
  )
}

///|
/// Matches Rust's border_e helper.
pub fn[T : IntoValue] Element::border_e(self : Element, value : T) -> Element {
  self.property(@protocol.BORDER_END_WIDTH, IntoValue::into_value(value))
}

///|
/// Matches Rust's border_e helper.
pub fn[T : IntoValue] Style::border_e(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_END_WIDTH,
    "borderEndWidth",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's border_s helper.
pub fn[T : IntoValue] Element::border_s(self : Element, value : T) -> Element {
  self.property(@protocol.BORDER_START_WIDTH, IntoValue::into_value(value))
}

///|
/// Matches Rust's border_s helper.
pub fn[T : IntoValue] Style::border_s(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_START_WIDTH,
    "borderStartWidth",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's border_solid helper.
pub fn Element::border_solid(self : Element) -> Element {
  self.property(@protocol.BORDER_STYLE, IntoValue::into_value("solid"))
}

///|
/// Matches Rust's border_solid helper.
pub fn Style::border_solid(self : Style) -> Style {
  self.property(
    @protocol.BORDER_STYLE,
    "borderStyle",
    IntoValue::into_value("solid"),
  )
}

///|
/// Matches Rust's col_end helper.
pub fn[T : IntoValue] Element::col_end(self : Element, value : T) -> Element {
  self.property(@protocol.GRID_COLUMN_END, IntoValue::into_value(value))
}

///|
/// Matches Rust's col_end helper.
pub fn[T : IntoValue] Style::col_end(self : Style, value : T) -> Style {
  self.property(
    @protocol.GRID_COLUMN_END,
    "gridColumnEnd",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's col_end_auto helper.
pub fn Element::col_end_auto(self : Element) -> Element {
  self.property(@protocol.GRID_COLUMN_END, IntoValue::into_value(0))
}

///|
/// Matches Rust's col_end_auto helper.
pub fn Style::col_end_auto(self : Style) -> Style {
  self.property(
    @protocol.GRID_COLUMN_END,
    "gridColumnEnd",
    IntoValue::into_value(0),
  )
}

///|
/// Matches Rust's col_span helper.
pub fn[T : IntoValue] Element::col_span(self : Element, value : T) -> Element {
  self
  .property(@protocol.GRID_COLUMN_START, @protocol.Clear)
  .property(@protocol.GRID_COLUMN_END, @protocol.Clear)
  .property(@protocol.GRID_COLUMN_SPAN, IntoValue::into_value(value))
}

///|
/// Matches Rust's col_span helper.
pub fn[T : IntoValue] Style::col_span(self : Style, value : T) -> Style {
  self
  .property(@protocol.GRID_COLUMN_START, "gridColumnStart", @protocol.Clear)
  .property(@protocol.GRID_COLUMN_END, "gridColumnEnd", @protocol.Clear)
  .property(
    @protocol.GRID_COLUMN_SPAN,
    "gridColumnSpan",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's col_span_full helper.
pub fn Element::col_span_full(self : Element) -> Element {
  self
  .property(@protocol.GRID_COLUMN_SPAN, @protocol.Clear)
  .property(@protocol.GRID_COLUMN_START, IntoValue::into_value(1))
  .property(@protocol.GRID_COLUMN_END, IntoValue::into_value(-1))
}

///|
/// Matches Rust's col_span_full helper.
pub fn Style::col_span_full(self : Style) -> Style {
  self
  .property(@protocol.GRID_COLUMN_SPAN, "gridColumnSpan", @protocol.Clear)
  .property(
    @protocol.GRID_COLUMN_START,
    "gridColumnStart",
    IntoValue::into_value(1),
  )
  .property(
    @protocol.GRID_COLUMN_END,
    "gridColumnEnd",
    IntoValue::into_value(-1),
  )
}

///|
/// Matches Rust's col_start helper.
pub fn[T : IntoValue] Element::col_start(self : Element, value : T) -> Element {
  self.property(@protocol.GRID_COLUMN_START, IntoValue::into_value(value))
}

///|
/// Matches Rust's col_start helper.
pub fn[T : IntoValue] Style::col_start(self : Style, value : T) -> Style {
  self.property(
    @protocol.GRID_COLUMN_START,
    "gridColumnStart",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's col_start_auto helper.
pub fn Element::col_start_auto(self : Element) -> Element {
  self.property(@protocol.GRID_COLUMN_START, IntoValue::into_value(0))
}

///|
/// Matches Rust's col_start_auto helper.
pub fn Style::col_start_auto(self : Style) -> Style {
  self.property(
    @protocol.GRID_COLUMN_START,
    "gridColumnStart",
    IntoValue::into_value(0),
  )
}

///|
/// Matches Rust's content_around helper.
pub fn Element::content_around(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("space-around"))
}

///|
/// Matches Rust's content_around helper.
pub fn Style::content_around(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("space-around"),
  )
}

///|
/// Matches Rust's content_between helper.
pub fn Element::content_between(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("space-between"))
}

///|
/// Matches Rust's content_between helper.
pub fn Style::content_between(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("space-between"),
  )
}

///|
/// Matches Rust's content_center helper.
pub fn Element::content_center(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("center"))
}

///|
/// Matches Rust's content_center helper.
pub fn Style::content_center(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("center"),
  )
}

///|
/// Matches Rust's content_end helper.
pub fn Element::content_end(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("flex-end"))
}

///|
/// Matches Rust's content_end helper.
pub fn Style::content_end(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("flex-end"),
  )
}

///|
/// Matches Rust's content_evenly helper.
pub fn Element::content_evenly(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("space-evenly"))
}

///|
/// Matches Rust's content_evenly helper.
pub fn Style::content_evenly(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("space-evenly"),
  )
}

///|
/// Matches Rust's content_normal helper.
pub fn Element::content_normal(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("normal"))
}

///|
/// Matches Rust's content_normal helper.
pub fn Style::content_normal(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("normal"),
  )
}

///|
/// Matches Rust's content_start helper.
pub fn Element::content_start(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("flex-start"))
}

///|
/// Matches Rust's content_start helper.
pub fn Style::content_start(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("flex-start"),
  )
}

///|
/// Matches Rust's content_stretch helper.
pub fn Element::content_stretch(self : Element) -> Element {
  self.property(@protocol.ALIGN_CONTENT, IntoValue::into_value("stretch"))
}

///|
/// Matches Rust's content_stretch helper.
pub fn Style::content_stretch(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_CONTENT,
    "alignContent",
    IntoValue::into_value("stretch"),
  )
}

///|
/// Matches Rust's cursor_alias helper.
pub fn Element::cursor_alias(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("drag-link"))
}

///|
/// Matches Rust's cursor_alias helper.
pub fn Style::cursor_alias(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("drag-link"))
}

///|
/// Matches Rust's cursor_col_resize helper.
pub fn Element::cursor_col_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-column"))
}

///|
/// Matches Rust's cursor_col_resize helper.
pub fn Style::cursor_col_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-column"),
  )
}

///|
/// Matches Rust's cursor_context_menu helper.
pub fn Element::cursor_context_menu(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("contextual-menu"))
}

///|
/// Matches Rust's cursor_context_menu helper.
pub fn Style::cursor_context_menu(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("contextual-menu"),
  )
}

///|
/// Matches Rust's cursor_copy helper.
pub fn Element::cursor_copy(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("drag-copy"))
}

///|
/// Matches Rust's cursor_copy helper.
pub fn Style::cursor_copy(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("drag-copy"))
}

///|
/// Matches Rust's cursor_crosshair helper.
pub fn Element::cursor_crosshair(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("crosshair"))
}

///|
/// Matches Rust's cursor_crosshair helper.
pub fn Style::cursor_crosshair(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("crosshair"))
}

///|
/// Matches Rust's cursor_default helper.
pub fn Element::cursor_default(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("arrow"))
}

///|
/// Matches Rust's cursor_default helper.
pub fn Style::cursor_default(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("arrow"))
}

///|
/// Matches Rust's cursor_e_resize helper.
pub fn Element::cursor_e_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-right"))
}

///|
/// Matches Rust's cursor_e_resize helper.
pub fn Style::cursor_e_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-right"),
  )
}

///|
/// Matches Rust's cursor_ew_resize helper.
pub fn Element::cursor_ew_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-left-right"))
}

///|
/// Matches Rust's cursor_ew_resize helper.
pub fn Style::cursor_ew_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-left-right"),
  )
}

///|
/// Matches Rust's cursor_grab helper.
pub fn Element::cursor_grab(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("open-hand"))
}

///|
/// Matches Rust's cursor_grab helper.
pub fn Style::cursor_grab(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("open-hand"))
}

///|
/// Matches Rust's cursor_grabbing helper.
pub fn Element::cursor_grabbing(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("closed-hand"))
}

///|
/// Matches Rust's cursor_grabbing helper.
pub fn Style::cursor_grabbing(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("closed-hand"),
  )
}

///|
/// Matches Rust's cursor_move helper.
pub fn Element::cursor_move(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("closed-hand"))
}

///|
/// Matches Rust's cursor_move helper.
pub fn Style::cursor_move(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("closed-hand"),
  )
}

///|
/// Matches Rust's cursor_n_resize helper.
pub fn Element::cursor_n_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-up"))
}

///|
/// Matches Rust's cursor_n_resize helper.
pub fn Style::cursor_n_resize(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("resize-up"))
}

///|
/// Matches Rust's cursor_nesw_resize helper.
pub fn Element::cursor_nesw_resize(self : Element) -> Element {
  self.property(
    @protocol.CURSOR,
    IntoValue::into_value("resize-up-right-down-left"),
  )
}

///|
/// Matches Rust's cursor_nesw_resize helper.
pub fn Style::cursor_nesw_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-up-right-down-left"),
  )
}

///|
/// Matches Rust's cursor_no_drop helper.
pub fn Element::cursor_no_drop(self : Element) -> Element {
  self.property(
    @protocol.CURSOR,
    IntoValue::into_value("operation-not-allowed"),
  )
}

///|
/// Matches Rust's cursor_no_drop helper.
pub fn Style::cursor_no_drop(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("operation-not-allowed"),
  )
}

///|
/// Matches Rust's cursor_not_allowed helper.
pub fn Element::cursor_not_allowed(self : Element) -> Element {
  self.property(
    @protocol.CURSOR,
    IntoValue::into_value("operation-not-allowed"),
  )
}

///|
/// Matches Rust's cursor_not_allowed helper.
pub fn Style::cursor_not_allowed(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("operation-not-allowed"),
  )
}

///|
/// Matches Rust's cursor_ns_resize helper.
pub fn Element::cursor_ns_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-up-down"))
}

///|
/// Matches Rust's cursor_ns_resize helper.
pub fn Style::cursor_ns_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-up-down"),
  )
}

///|
/// Matches Rust's cursor_nwse_resize helper.
pub fn Element::cursor_nwse_resize(self : Element) -> Element {
  self.property(
    @protocol.CURSOR,
    IntoValue::into_value("resize-up-left-down-right"),
  )
}

///|
/// Matches Rust's cursor_nwse_resize helper.
pub fn Style::cursor_nwse_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-up-left-down-right"),
  )
}

///|
/// Matches Rust's cursor_pointer helper.
pub fn Element::cursor_pointer(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("pointing-hand"))
}

///|
/// Matches Rust's cursor_pointer helper.
pub fn Style::cursor_pointer(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("pointing-hand"),
  )
}

///|
/// Matches Rust's cursor_row_resize helper.
pub fn Element::cursor_row_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-row"))
}

///|
/// Matches Rust's cursor_row_resize helper.
pub fn Style::cursor_row_resize(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("resize-row"))
}

///|
/// Matches Rust's cursor_s_resize helper.
pub fn Element::cursor_s_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-down"))
}

///|
/// Matches Rust's cursor_s_resize helper.
pub fn Style::cursor_s_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-down"),
  )
}

///|
/// Matches Rust's cursor_text helper.
pub fn Element::cursor_text(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("ibeam"))
}

///|
/// Matches Rust's cursor_text helper.
pub fn Style::cursor_text(self : Style) -> Style {
  self.property(@protocol.CURSOR, "cursor", IntoValue::into_value("ibeam"))
}

///|
/// Matches Rust's cursor_vertical_text helper.
pub fn Element::cursor_vertical_text(self : Element) -> Element {
  self.property(
    @protocol.CURSOR,
    IntoValue::into_value("ibeam-cursor-for-vertical-layout"),
  )
}

///|
/// Matches Rust's cursor_vertical_text helper.
pub fn Style::cursor_vertical_text(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("ibeam-cursor-for-vertical-layout"),
  )
}

///|
/// Matches Rust's cursor_w_resize helper.
pub fn Element::cursor_w_resize(self : Element) -> Element {
  self.property(@protocol.CURSOR, IntoValue::into_value("resize-left"))
}

///|
/// Matches Rust's cursor_w_resize helper.
pub fn Style::cursor_w_resize(self : Style) -> Style {
  self.property(
    @protocol.CURSOR,
    "cursor",
    IntoValue::into_value("resize-left"),
  )
}

///|
/// Matches Rust's flex helper.
pub fn Element::flex(self : Element) -> Element {
  self.property(@protocol.DISPLAY, IntoValue::into_value("flex"))
}

///|
/// Matches Rust's flex helper.
pub fn Style::flex(self : Style) -> Style {
  self.property(@protocol.DISPLAY, "display", IntoValue::into_value("flex"))
}

///|
/// Matches Rust's flex_1 helper.
pub fn Element::flex_1(self : Element) -> Element {
  self
  .property(@protocol.FLEX_GROW, IntoValue::into_value(1))
  .property(@protocol.FLEX_SHRINK, IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_1 helper.
pub fn Style::flex_1(self : Style) -> Style {
  self
  .property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(1))
  .property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, "flexBasis", IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_auto helper.
pub fn Element::flex_auto(self : Element) -> Element {
  self
  .property(@protocol.FLEX_GROW, IntoValue::into_value(1))
  .property(@protocol.FLEX_SHRINK, IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_auto helper.
pub fn Style::flex_auto(self : Style) -> Style {
  self
  .property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(1))
  .property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, "flexBasis", IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_basis_auto helper.
pub fn Element::flex_basis_auto(self : Element) -> Element {
  self.property(@protocol.FLEX_BASIS, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_basis_auto helper.
pub fn Style::flex_basis_auto(self : Style) -> Style {
  self.property(
    @protocol.FLEX_BASIS,
    "flexBasis",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's flex_col helper.
pub fn Element::flex_col(self : Element) -> Element {
  self
  .property(@protocol.DISPLAY, IntoValue::into_value("flex"))
  .property(@protocol.FLEX_DIRECTION, IntoValue::into_value("column"))
}

///|
/// Matches Rust's flex_col helper.
pub fn Style::flex_col(self : Style) -> Style {
  self
  .property(@protocol.DISPLAY, "display", IntoValue::into_value("flex"))
  .property(
    @protocol.FLEX_DIRECTION,
    "flexDirection",
    IntoValue::into_value("column"),
  )
}

///|
/// Matches Rust's flex_col_reverse helper.
pub fn Element::flex_col_reverse(self : Element) -> Element {
  self
  .property(@protocol.DISPLAY, IntoValue::into_value("flex"))
  .property(@protocol.FLEX_DIRECTION, IntoValue::into_value("column-reverse"))
}

///|
/// Matches Rust's flex_col_reverse helper.
pub fn Style::flex_col_reverse(self : Style) -> Style {
  self
  .property(@protocol.DISPLAY, "display", IntoValue::into_value("flex"))
  .property(
    @protocol.FLEX_DIRECTION,
    "flexDirection",
    IntoValue::into_value("column-reverse"),
  )
}

///|
/// Matches Rust's flex_grow_0 helper.
pub fn Element::flex_grow_0(self : Element) -> Element {
  self.property(@protocol.FLEX_GROW, IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_grow_0 helper.
pub fn Style::flex_grow_0(self : Style) -> Style {
  self.property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_grow_1 helper.
pub fn Element::flex_grow_1(self : Element) -> Element {
  self.property(@protocol.FLEX_GROW, IntoValue::into_value(1))
}

///|
/// Matches Rust's flex_grow_1 helper.
pub fn Style::flex_grow_1(self : Style) -> Style {
  self.property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(1))
}

///|
/// Matches Rust's flex_initial helper.
pub fn Element::flex_initial(self : Element) -> Element {
  self
  .property(@protocol.FLEX_GROW, IntoValue::into_value(0))
  .property(@protocol.FLEX_SHRINK, IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_initial helper.
pub fn Style::flex_initial(self : Style) -> Style {
  self
  .property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(0))
  .property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(1))
  .property(@protocol.FLEX_BASIS, "flexBasis", IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_none helper.
pub fn Element::flex_none(self : Element) -> Element {
  self
  .property(@protocol.FLEX_GROW, IntoValue::into_value(0))
  .property(@protocol.FLEX_SHRINK, IntoValue::into_value(0))
  .property(@protocol.FLEX_BASIS, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_none helper.
pub fn Style::flex_none(self : Style) -> Style {
  self
  .property(@protocol.FLEX_GROW, "flexGrow", IntoValue::into_value(0))
  .property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(0))
  .property(@protocol.FLEX_BASIS, "flexBasis", IntoValue::into_value("auto"))
}

///|
/// Matches Rust's flex_nowrap helper.
pub fn Element::flex_nowrap(self : Element) -> Element {
  self.property(@protocol.FLEX_WRAP, IntoValue::into_value("nowrap"))
}

///|
/// Matches Rust's flex_nowrap helper.
pub fn Style::flex_nowrap(self : Style) -> Style {
  self.property(
    @protocol.FLEX_WRAP,
    "flexWrap",
    IntoValue::into_value("nowrap"),
  )
}

///|
/// Matches Rust's flex_row helper.
pub fn Element::flex_row(self : Element) -> Element {
  self
  .property(@protocol.DISPLAY, IntoValue::into_value("flex"))
  .property(@protocol.FLEX_DIRECTION, IntoValue::into_value("row"))
}

///|
/// Matches Rust's flex_row helper.
pub fn Style::flex_row(self : Style) -> Style {
  self
  .property(@protocol.DISPLAY, "display", IntoValue::into_value("flex"))
  .property(
    @protocol.FLEX_DIRECTION,
    "flexDirection",
    IntoValue::into_value("row"),
  )
}

///|
/// Matches Rust's flex_row_reverse helper.
pub fn Element::flex_row_reverse(self : Element) -> Element {
  self
  .property(@protocol.DISPLAY, IntoValue::into_value("flex"))
  .property(@protocol.FLEX_DIRECTION, IntoValue::into_value("row-reverse"))
}

///|
/// Matches Rust's flex_row_reverse helper.
pub fn Style::flex_row_reverse(self : Style) -> Style {
  self
  .property(@protocol.DISPLAY, "display", IntoValue::into_value("flex"))
  .property(
    @protocol.FLEX_DIRECTION,
    "flexDirection",
    IntoValue::into_value("row-reverse"),
  )
}

///|
/// Matches Rust's flex_shrink_0 helper.
pub fn Element::flex_shrink_0(self : Element) -> Element {
  self.property(@protocol.FLEX_SHRINK, IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_shrink_0 helper.
pub fn Style::flex_shrink_0(self : Style) -> Style {
  self.property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(0))
}

///|
/// Matches Rust's flex_shrink_1 helper.
pub fn Element::flex_shrink_1(self : Element) -> Element {
  self.property(@protocol.FLEX_SHRINK, IntoValue::into_value(1))
}

///|
/// Matches Rust's flex_shrink_1 helper.
pub fn Style::flex_shrink_1(self : Style) -> Style {
  self.property(@protocol.FLEX_SHRINK, "flexShrink", IntoValue::into_value(1))
}

///|
/// Matches Rust's flex_wrap helper.
pub fn Element::flex_wrap(self : Element) -> Element {
  self.property(@protocol.FLEX_WRAP, IntoValue::into_value("wrap"))
}

///|
/// Matches Rust's flex_wrap helper.
pub fn Style::flex_wrap(self : Style) -> Style {
  self.property(@protocol.FLEX_WRAP, "flexWrap", IntoValue::into_value("wrap"))
}

///|
/// Matches Rust's flex_wrap_reverse helper.
pub fn Element::flex_wrap_reverse(self : Element) -> Element {
  self.property(@protocol.FLEX_WRAP, IntoValue::into_value("wrap-reverse"))
}

///|
/// Matches Rust's flex_wrap_reverse helper.
pub fn Style::flex_wrap_reverse(self : Style) -> Style {
  self.property(
    @protocol.FLEX_WRAP,
    "flexWrap",
    IntoValue::into_value("wrap-reverse"),
  )
}

///|
/// Matches Rust's font_bold helper.
pub fn Element::font_bold(self : Element) -> Element {
  self.property(@protocol.FONT_WEIGHT, IntoValue::into_value(700))
}

///|
/// Matches Rust's font_bold helper.
pub fn Style::font_bold(self : Style) -> Style {
  self.property(@protocol.FONT_WEIGHT, "fontWeight", IntoValue::into_value(700))
}

///|
/// Matches Rust's font_medium helper.
pub fn Element::font_medium(self : Element) -> Element {
  self.property(@protocol.FONT_WEIGHT, IntoValue::into_value(500))
}

///|
/// Matches Rust's font_medium helper.
pub fn Style::font_medium(self : Style) -> Style {
  self.property(@protocol.FONT_WEIGHT, "fontWeight", IntoValue::into_value(500))
}

///|
/// Matches Rust's font_normal helper.
pub fn Element::font_normal(self : Element) -> Element {
  self.property(@protocol.FONT_WEIGHT, IntoValue::into_value(400))
}

///|
/// Matches Rust's font_normal helper.
pub fn Style::font_normal(self : Style) -> Style {
  self.property(@protocol.FONT_WEIGHT, "fontWeight", IntoValue::into_value(400))
}

///|
/// Matches Rust's font_semibold helper.
pub fn Element::font_semibold(self : Element) -> Element {
  self.property(@protocol.FONT_WEIGHT, IntoValue::into_value(600))
}

///|
/// Matches Rust's font_semibold helper.
pub fn Style::font_semibold(self : Style) -> Style {
  self.property(@protocol.FONT_WEIGHT, "fontWeight", IntoValue::into_value(600))
}

///|
/// Matches Rust's gap_1 helper.
pub fn Element::gap_1(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_1 helper.
pub fn Style::gap_1(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_2 helper.
pub fn Element::gap_2(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_2 helper.
pub fn Style::gap_2(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_3 helper.
pub fn Element::gap_3(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_3 helper.
pub fn Style::gap_3(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_4 helper.
pub fn Element::gap_4(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_4 helper.
pub fn Style::gap_4(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_5 helper.
pub fn Element::gap_5(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_5 helper.
pub fn Style::gap_5(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_6 helper.
pub fn Element::gap_6(self : Element) -> Element {
  self.property(@protocol.GAP, IntoValue::into_value(24))
}

///|
/// Matches Rust's gap_6 helper.
pub fn Style::gap_6(self : Style) -> Style {
  self.property(@protocol.GAP, "gap", IntoValue::into_value(24))
}

///|
/// Matches Rust's gap_x helper.
pub fn[T : IntoValue] Element::gap_x(self : Element, value : T) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(value))
}

///|
/// Matches Rust's gap_x helper.
pub fn[T : IntoValue] Style::gap_x(self : Style, value : T) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(value))
}

///|
/// Matches Rust's gap_x_0 helper.
pub fn Element::gap_x_0(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(0))
}

///|
/// Matches Rust's gap_x_0 helper.
pub fn Style::gap_x_0(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(0))
}

///|
/// Matches Rust's gap_x_1 helper.
pub fn Element::gap_x_1(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_x_1 helper.
pub fn Style::gap_x_1(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_x_2 helper.
pub fn Element::gap_x_2(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_x_2 helper.
pub fn Style::gap_x_2(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_x_3 helper.
pub fn Element::gap_x_3(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_x_3 helper.
pub fn Style::gap_x_3(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_x_4 helper.
pub fn Element::gap_x_4(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_x_4 helper.
pub fn Style::gap_x_4(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_x_5 helper.
pub fn Element::gap_x_5(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_x_5 helper.
pub fn Style::gap_x_5(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_x_6 helper.
pub fn Element::gap_x_6(self : Element) -> Element {
  self.property(@protocol.COLUMN_GAP, IntoValue::into_value(24))
}

///|
/// Matches Rust's gap_x_6 helper.
pub fn Style::gap_x_6(self : Style) -> Style {
  self.property(@protocol.COLUMN_GAP, "columnGap", IntoValue::into_value(24))
}

///|
/// Matches Rust's gap_y helper.
pub fn[T : IntoValue] Element::gap_y(self : Element, value : T) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(value))
}

///|
/// Matches Rust's gap_y helper.
pub fn[T : IntoValue] Style::gap_y(self : Style, value : T) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(value))
}

///|
/// Matches Rust's gap_y_0 helper.
pub fn Element::gap_y_0(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(0))
}

///|
/// Matches Rust's gap_y_0 helper.
pub fn Style::gap_y_0(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(0))
}

///|
/// Matches Rust's gap_y_1 helper.
pub fn Element::gap_y_1(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_y_1 helper.
pub fn Style::gap_y_1(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(4))
}

///|
/// Matches Rust's gap_y_2 helper.
pub fn Element::gap_y_2(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_y_2 helper.
pub fn Style::gap_y_2(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(8))
}

///|
/// Matches Rust's gap_y_3 helper.
pub fn Element::gap_y_3(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_y_3 helper.
pub fn Style::gap_y_3(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(12))
}

///|
/// Matches Rust's gap_y_4 helper.
pub fn Element::gap_y_4(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_y_4 helper.
pub fn Style::gap_y_4(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(16))
}

///|
/// Matches Rust's gap_y_5 helper.
pub fn Element::gap_y_5(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_y_5 helper.
pub fn Style::gap_y_5(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(20))
}

///|
/// Matches Rust's gap_y_6 helper.
pub fn Element::gap_y_6(self : Element) -> Element {
  self.property(@protocol.ROW_GAP, IntoValue::into_value(24))
}

///|
/// Matches Rust's gap_y_6 helper.
pub fn Style::gap_y_6(self : Style) -> Style {
  self.property(@protocol.ROW_GAP, "rowGap", IntoValue::into_value(24))
}

///|
/// Matches Rust's grid helper.
pub fn Element::grid(self : Element) -> Element {
  self.property(@protocol.DISPLAY, IntoValue::into_value("grid"))
}

///|
/// Matches Rust's grid helper.
pub fn Style::grid(self : Style) -> Style {
  self.property(@protocol.DISPLAY, "display", IntoValue::into_value("grid"))
}

///|
/// Matches Rust's grid_cols helper.
pub fn Element::grid_cols(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    IntoValue::into_value(layout_grid_tracks(count, "0", "1fr")),
  )
}

///|
/// Matches Rust's grid_cols helper.
pub fn Style::grid_cols(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    "gridTemplateColumns",
    IntoValue::into_value(layout_grid_tracks(count, "0", "1fr")),
  )
}

///|
/// Matches Rust's grid_cols_max_content helper.
pub fn Element::grid_cols_max_content(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    IntoValue::into_value(layout_grid_tracks(count, "0", "max-content")),
  )
}

///|
/// Matches Rust's grid_cols_max_content helper.
pub fn Style::grid_cols_max_content(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    "gridTemplateColumns",
    IntoValue::into_value(layout_grid_tracks(count, "0", "max-content")),
  )
}

///|
/// Matches Rust's grid_cols_min_content helper.
pub fn Element::grid_cols_min_content(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    IntoValue::into_value(layout_grid_tracks(count, "min-content", "1fr")),
  )
}

///|
/// Matches Rust's grid_cols_min_content helper.
pub fn Style::grid_cols_min_content(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_COLUMNS,
    "gridTemplateColumns",
    IntoValue::into_value(layout_grid_tracks(count, "min-content", "1fr")),
  )
}

///|
/// Matches Rust's grid_flow_col helper.
pub fn Element::grid_flow_col(self : Element) -> Element {
  self.property(@protocol.GRID_AUTO_FLOW, IntoValue::into_value("column"))
}

///|
/// Matches Rust's grid_flow_col helper.
pub fn Style::grid_flow_col(self : Style) -> Style {
  self.property(
    @protocol.GRID_AUTO_FLOW,
    "gridAutoFlow",
    IntoValue::into_value("column"),
  )
}

///|
/// Matches Rust's grid_flow_col_dense helper.
pub fn Element::grid_flow_col_dense(self : Element) -> Element {
  self.property(@protocol.GRID_AUTO_FLOW, IntoValue::into_value("column dense"))
}

///|
/// Matches Rust's grid_flow_col_dense helper.
pub fn Style::grid_flow_col_dense(self : Style) -> Style {
  self.property(
    @protocol.GRID_AUTO_FLOW,
    "gridAutoFlow",
    IntoValue::into_value("column dense"),
  )
}

///|
/// Matches Rust's grid_flow_row helper.
pub fn Element::grid_flow_row(self : Element) -> Element {
  self.property(@protocol.GRID_AUTO_FLOW, IntoValue::into_value("row"))
}

///|
/// Matches Rust's grid_flow_row helper.
pub fn Style::grid_flow_row(self : Style) -> Style {
  self.property(
    @protocol.GRID_AUTO_FLOW,
    "gridAutoFlow",
    IntoValue::into_value("row"),
  )
}

///|
/// Matches Rust's grid_flow_row_dense helper.
pub fn Element::grid_flow_row_dense(self : Element) -> Element {
  self.property(@protocol.GRID_AUTO_FLOW, IntoValue::into_value("row dense"))
}

///|
/// Matches Rust's grid_flow_row_dense helper.
pub fn Style::grid_flow_row_dense(self : Style) -> Style {
  self.property(
    @protocol.GRID_AUTO_FLOW,
    "gridAutoFlow",
    IntoValue::into_value("row dense"),
  )
}

///|
/// Matches Rust's grid_rows helper.
pub fn Element::grid_rows(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    IntoValue::into_value(layout_grid_tracks(count, "0", "1fr")),
  )
}

///|
/// Matches Rust's grid_rows helper.
pub fn Style::grid_rows(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    "gridTemplateRows",
    IntoValue::into_value(layout_grid_tracks(count, "0", "1fr")),
  )
}

///|
/// Matches Rust's grid_rows_max_content helper.
pub fn Element::grid_rows_max_content(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    IntoValue::into_value(layout_grid_tracks(count, "0", "max-content")),
  )
}

///|
/// Matches Rust's grid_rows_max_content helper.
pub fn Style::grid_rows_max_content(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    "gridTemplateRows",
    IntoValue::into_value(layout_grid_tracks(count, "0", "max-content")),
  )
}

///|
/// Matches Rust's grid_rows_min_content helper.
pub fn Element::grid_rows_min_content(self : Element, count : Int) -> Element {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    IntoValue::into_value(layout_grid_tracks(count, "min-content", "1fr")),
  )
}

///|
/// Matches Rust's grid_rows_min_content helper.
pub fn Style::grid_rows_min_content(self : Style, count : Int) -> Style {
  self.property(
    @protocol.GRID_TEMPLATE_ROWS,
    "gridTemplateRows",
    IntoValue::into_value(layout_grid_tracks(count, "min-content", "1fr")),
  )
}

///|
/// Matches Rust's h helper.
pub fn[T : IntoValue] Element::h(self : Element, value : T) -> Element {
  self.property(@protocol.HEIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's h helper.
pub fn[T : IntoValue] Style::h(self : Style, value : T) -> Style {
  self.property(@protocol.HEIGHT, "height", IntoValue::into_value(value))
}

///|
/// Matches Rust's h_10 helper.
pub fn Element::h_10(self : Element) -> Element {
  self.property(@protocol.HEIGHT, IntoValue::into_value(40))
}

///|
/// Matches Rust's h_10 helper.
pub fn Style::h_10(self : Style) -> Style {
  self.property(@protocol.HEIGHT, "height", IntoValue::into_value(40))
}

///|
/// Matches Rust's h_12 helper.
pub fn Element::h_12(self : Element) -> Element {
  self.property(@protocol.HEIGHT, IntoValue::into_value(48))
}

///|
/// Matches Rust's h_12 helper.
pub fn Style::h_12(self : Style) -> Style {
  self.property(@protocol.HEIGHT, "height", IntoValue::into_value(48))
}

///|
/// Matches Rust's h_8 helper.
pub fn Element::h_8(self : Element) -> Element {
  self.property(@protocol.HEIGHT, IntoValue::into_value(32))
}

///|
/// Matches Rust's h_8 helper.
pub fn Style::h_8(self : Style) -> Style {
  self.property(@protocol.HEIGHT, "height", IntoValue::into_value(32))
}

///|
/// Matches Rust's h_fraction helper.
pub fn Element::h_fraction(self : Element, fraction : Float) -> Element {
  self.property(
    @protocol.HEIGHT,
    IntoValue::into_value(layout_fraction(fraction)),
  )
}

///|
/// Matches Rust's h_fraction helper.
pub fn Style::h_fraction(self : Style, fraction : Float) -> Style {
  self.property(
    @protocol.HEIGHT,
    "height",
    IntoValue::into_value(layout_fraction(fraction)),
  )
}

///|
/// Matches Rust's h_full helper.
pub fn Element::h_full(self : Element) -> Element {
  self.property(@protocol.HEIGHT, IntoValue::into_value("100%"))
}

///|
/// Matches Rust's h_full helper.
pub fn Style::h_full(self : Style) -> Style {
  self.property(@protocol.HEIGHT, "height", IntoValue::into_value("100%"))
}

///|
/// Matches Rust's hidden helper.
pub fn Element::hidden(self : Element) -> Element {
  self.property(@protocol.DISPLAY, IntoValue::into_value("none"))
}

///|
/// Matches Rust's hidden helper.
pub fn Style::hidden(self : Style) -> Style {
  self.property(@protocol.DISPLAY, "display", IntoValue::into_value("none"))
}

///|
/// Matches Rust's inset_0 helper.
pub fn Element::inset_0(self : Element) -> Element {
  self
  .property(@protocol.TOP, IntoValue::into_value(0))
  .property(@protocol.RIGHT, IntoValue::into_value(0))
  .property(@protocol.BOTTOM, IntoValue::into_value(0))
  .property(@protocol.LEFT, IntoValue::into_value(0))
}

///|
/// Matches Rust's inset_0 helper.
pub fn Style::inset_0(self : Style) -> Style {
  self
  .property(@protocol.TOP, "top", IntoValue::into_value(0))
  .property(@protocol.RIGHT, "right", IntoValue::into_value(0))
  .property(@protocol.BOTTOM, "bottom", IntoValue::into_value(0))
  .property(@protocol.LEFT, "left", IntoValue::into_value(0))
}

///|
/// Matches Rust's invisible helper.
pub fn Element::invisible(self : Element) -> Element {
  self.property(@protocol.VISIBILITY, IntoValue::into_value("hidden"))
}

///|
/// Matches Rust's invisible helper.
pub fn Style::invisible(self : Style) -> Style {
  self.property(
    @protocol.VISIBILITY,
    "visibility",
    IntoValue::into_value("hidden"),
  )
}

///|
/// Matches Rust's items_baseline helper.
pub fn Element::items_baseline(self : Element) -> Element {
  self.property(@protocol.ALIGN_ITEMS, IntoValue::into_value("baseline"))
}

///|
/// Matches Rust's items_baseline helper.
pub fn Style::items_baseline(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_ITEMS,
    "alignItems",
    IntoValue::into_value("baseline"),
  )
}

///|
/// Matches Rust's items_center helper.
pub fn Element::items_center(self : Element) -> Element {
  self.property(@protocol.ALIGN_ITEMS, IntoValue::into_value("center"))
}

///|
/// Matches Rust's items_center helper.
pub fn Style::items_center(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_ITEMS,
    "alignItems",
    IntoValue::into_value("center"),
  )
}

///|
/// Matches Rust's items_end helper.
pub fn Element::items_end(self : Element) -> Element {
  self.property(@protocol.ALIGN_ITEMS, IntoValue::into_value("flex-end"))
}

///|
/// Matches Rust's items_end helper.
pub fn Style::items_end(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_ITEMS,
    "alignItems",
    IntoValue::into_value("flex-end"),
  )
}

///|
/// Matches Rust's items_start helper.
pub fn Element::items_start(self : Element) -> Element {
  self.property(@protocol.ALIGN_ITEMS, IntoValue::into_value("flex-start"))
}

///|
/// Matches Rust's items_start helper.
pub fn Style::items_start(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_ITEMS,
    "alignItems",
    IntoValue::into_value("flex-start"),
  )
}

///|
/// Matches Rust's items_stretch helper.
pub fn Element::items_stretch(self : Element) -> Element {
  self.property(@protocol.ALIGN_ITEMS, IntoValue::into_value("stretch"))
}

///|
/// Matches Rust's items_stretch helper.
pub fn Style::items_stretch(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_ITEMS,
    "alignItems",
    IntoValue::into_value("stretch"),
  )
}

///|
/// Matches Rust's justify_around helper.
pub fn Element::justify_around(self : Element) -> Element {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    IntoValue::into_value("space-around"),
  )
}

///|
/// Matches Rust's justify_around helper.
pub fn Style::justify_around(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("space-around"),
  )
}

///|
/// Matches Rust's justify_between helper.
pub fn Element::justify_between(self : Element) -> Element {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    IntoValue::into_value("space-between"),
  )
}

///|
/// Matches Rust's justify_between helper.
pub fn Style::justify_between(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("space-between"),
  )
}

///|
/// Matches Rust's justify_center helper.
pub fn Element::justify_center(self : Element) -> Element {
  self.property(@protocol.JUSTIFY_CONTENT, IntoValue::into_value("center"))
}

///|
/// Matches Rust's justify_center helper.
pub fn Style::justify_center(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("center"),
  )
}

///|
/// Matches Rust's justify_end helper.
pub fn Element::justify_end(self : Element) -> Element {
  self.property(@protocol.JUSTIFY_CONTENT, IntoValue::into_value("end"))
}

///|
/// Matches Rust's justify_end helper.
pub fn Style::justify_end(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("end"),
  )
}

///|
/// Matches Rust's justify_evenly helper.
pub fn Element::justify_evenly(self : Element) -> Element {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    IntoValue::into_value("space-evenly"),
  )
}

///|
/// Matches Rust's justify_evenly helper.
pub fn Style::justify_evenly(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("space-evenly"),
  )
}

///|
/// Matches Rust's justify_start helper.
pub fn Element::justify_start(self : Element) -> Element {
  self.property(@protocol.JUSTIFY_CONTENT, IntoValue::into_value("start"))
}

///|
/// Matches Rust's justify_start helper.
pub fn Style::justify_start(self : Style) -> Style {
  self.property(
    @protocol.JUSTIFY_CONTENT,
    "justifyContent",
    IntoValue::into_value("start"),
  )
}

///|
/// Matches Rust's ltr helper.
pub fn Element::ltr(self : Element) -> Element {
  self.property(@protocol.DIRECTION, IntoValue::into_value("ltr"))
}

///|
/// Matches Rust's ltr helper.
pub fn Style::ltr(self : Style) -> Style {
  self.property(@protocol.DIRECTION, "direction", IntoValue::into_value("ltr"))
}

///|
/// Matches Rust's m helper.
pub fn[T : IntoValue] Element::m(self : Element, value : T) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(value))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(value))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(value))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(value))
}

///|
/// Matches Rust's m helper.
pub fn[T : IntoValue] Style::m(self : Style, value : T) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(value))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(value))
  .property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(value),
  )
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(value))
}

///|
/// Matches Rust's m_0 helper.
pub fn Element::m_0(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(0))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(0))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(0))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(0))
}

///|
/// Matches Rust's m_0 helper.
pub fn Style::m_0(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(0))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(0))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(0))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(0))
}

///|
/// Matches Rust's m_1 helper.
pub fn Element::m_1(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(4))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(4))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(4))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(4))
}

///|
/// Matches Rust's m_1 helper.
pub fn Style::m_1(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(4))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(4))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(4))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(4))
}

///|
/// Matches Rust's m_10 helper.
pub fn Element::m_10(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(40))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(40))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(40))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(40))
}

///|
/// Matches Rust's m_10 helper.
pub fn Style::m_10(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(40))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(40))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(40))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(40))
}

///|
/// Matches Rust's m_12 helper.
pub fn Element::m_12(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(48))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(48))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(48))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(48))
}

///|
/// Matches Rust's m_12 helper.
pub fn Style::m_12(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(48))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(48))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(48))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(48))
}

///|
/// Matches Rust's m_16 helper.
pub fn Element::m_16(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(64))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(64))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(64))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(64))
}

///|
/// Matches Rust's m_16 helper.
pub fn Style::m_16(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(64))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(64))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(64))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(64))
}

///|
/// Matches Rust's m_2 helper.
pub fn Element::m_2(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(8))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(8))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(8))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(8))
}

///|
/// Matches Rust's m_2 helper.
pub fn Style::m_2(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(8))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(8))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(8))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(8))
}

///|
/// Matches Rust's m_20 helper.
pub fn Element::m_20(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(80))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(80))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(80))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(80))
}

///|
/// Matches Rust's m_20 helper.
pub fn Style::m_20(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(80))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(80))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(80))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(80))
}

///|
/// Matches Rust's m_24 helper.
pub fn Element::m_24(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(96))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(96))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(96))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(96))
}

///|
/// Matches Rust's m_24 helper.
pub fn Style::m_24(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(96))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(96))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(96))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(96))
}

///|
/// Matches Rust's m_3 helper.
pub fn Element::m_3(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(12))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(12))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(12))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(12))
}

///|
/// Matches Rust's m_3 helper.
pub fn Style::m_3(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(12))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(12))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(12))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(12))
}

///|
/// Matches Rust's m_32 helper.
pub fn Element::m_32(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(128))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(128))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(128))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(128))
}

///|
/// Matches Rust's m_32 helper.
pub fn Style::m_32(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(128))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(128))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(128))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(128))
}

///|
/// Matches Rust's m_4 helper.
pub fn Element::m_4(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(16))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(16))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(16))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(16))
}

///|
/// Matches Rust's m_4 helper.
pub fn Style::m_4(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(16))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(16))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(16))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(16))
}

///|
/// Matches Rust's m_5 helper.
pub fn Element::m_5(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(20))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(20))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(20))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(20))
}

///|
/// Matches Rust's m_5 helper.
pub fn Style::m_5(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(20))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(20))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(20))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(20))
}

///|
/// Matches Rust's m_6 helper.
pub fn Element::m_6(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(24))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(24))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(24))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(24))
}

///|
/// Matches Rust's m_6 helper.
pub fn Style::m_6(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(24))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(24))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(24))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(24))
}

///|
/// Matches Rust's m_8 helper.
pub fn Element::m_8(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(32))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(32))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(32))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(32))
}

///|
/// Matches Rust's m_8 helper.
pub fn Style::m_8(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(32))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(32))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(32))
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(32))
}

///|
/// Matches Rust's m_auto helper.
pub fn Element::m_auto(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value("auto"))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value("auto"))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value("auto"))
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's m_auto helper.
pub fn Style::m_auto(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value("auto"))
  .property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value("auto"),
  )
  .property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value("auto"),
  )
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value("auto"))
}

///|
/// Matches Rust's max_h helper.
pub fn[T : IntoValue] Element::max_h(self : Element, value : T) -> Element {
  self.property(@protocol.MAX_HEIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's max_h helper.
pub fn[T : IntoValue] Style::max_h(self : Style, value : T) -> Style {
  self.property(@protocol.MAX_HEIGHT, "maxHeight", IntoValue::into_value(value))
}

///|
/// Matches Rust's max_w helper.
pub fn[T : IntoValue] Element::max_w(self : Element, value : T) -> Element {
  self.property(@protocol.MAX_WIDTH, IntoValue::into_value(value))
}

///|
/// Matches Rust's max_w helper.
pub fn[T : IntoValue] Style::max_w(self : Style, value : T) -> Style {
  self.property(@protocol.MAX_WIDTH, "maxWidth", IntoValue::into_value(value))
}

///|
/// Matches Rust's mb helper.
pub fn[T : IntoValue] Element::mb(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(value))
}

///|
/// Matches Rust's mb helper.
pub fn[T : IntoValue] Style::mb(self : Style, value : T) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's mb_0 helper.
pub fn Element::mb_0(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(0))
}

///|
/// Matches Rust's mb_0 helper.
pub fn Style::mb_0(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(0),
  )
}

///|
/// Matches Rust's mb_1 helper.
pub fn Element::mb_1(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(4))
}

///|
/// Matches Rust's mb_1 helper.
pub fn Style::mb_1(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(4),
  )
}

///|
/// Matches Rust's mb_10 helper.
pub fn Element::mb_10(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(40))
}

///|
/// Matches Rust's mb_10 helper.
pub fn Style::mb_10(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(40),
  )
}

///|
/// Matches Rust's mb_12 helper.
pub fn Element::mb_12(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(48))
}

///|
/// Matches Rust's mb_12 helper.
pub fn Style::mb_12(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(48),
  )
}

///|
/// Matches Rust's mb_16 helper.
pub fn Element::mb_16(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(64))
}

///|
/// Matches Rust's mb_16 helper.
pub fn Style::mb_16(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(64),
  )
}

///|
/// Matches Rust's mb_2 helper.
pub fn Element::mb_2(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(8))
}

///|
/// Matches Rust's mb_2 helper.
pub fn Style::mb_2(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(8),
  )
}

///|
/// Matches Rust's mb_20 helper.
pub fn Element::mb_20(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(80))
}

///|
/// Matches Rust's mb_20 helper.
pub fn Style::mb_20(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(80),
  )
}

///|
/// Matches Rust's mb_24 helper.
pub fn Element::mb_24(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(96))
}

///|
/// Matches Rust's mb_24 helper.
pub fn Style::mb_24(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(96),
  )
}

///|
/// Matches Rust's mb_3 helper.
pub fn Element::mb_3(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(12))
}

///|
/// Matches Rust's mb_3 helper.
pub fn Style::mb_3(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(12),
  )
}

///|
/// Matches Rust's mb_32 helper.
pub fn Element::mb_32(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(128))
}

///|
/// Matches Rust's mb_32 helper.
pub fn Style::mb_32(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(128),
  )
}

///|
/// Matches Rust's mb_4 helper.
pub fn Element::mb_4(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(16))
}

///|
/// Matches Rust's mb_4 helper.
pub fn Style::mb_4(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(16),
  )
}

///|
/// Matches Rust's mb_5 helper.
pub fn Element::mb_5(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(20))
}

///|
/// Matches Rust's mb_5 helper.
pub fn Style::mb_5(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(20),
  )
}

///|
/// Matches Rust's mb_6 helper.
pub fn Element::mb_6(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(24))
}

///|
/// Matches Rust's mb_6 helper.
pub fn Style::mb_6(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(24),
  )
}

///|
/// Matches Rust's mb_8 helper.
pub fn Element::mb_8(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(32))
}

///|
/// Matches Rust's mb_8 helper.
pub fn Style::mb_8(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(32),
  )
}

///|
/// Matches Rust's mb_auto helper.
pub fn Element::mb_auto(self : Element) -> Element {
  self.property(@protocol.MARGIN_BOTTOM, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's mb_auto helper.
pub fn Style::mb_auto(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's me helper.
pub fn[T : IntoValue] Element::me(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_END, IntoValue::into_value(value))
}

///|
/// Matches Rust's me helper.
pub fn[T : IntoValue] Style::me(self : Style, value : T) -> Style {
  self.property(@protocol.MARGIN_END, "marginEnd", IntoValue::into_value(value))
}

///|
/// Matches Rust's min_h helper.
pub fn[T : IntoValue] Element::min_h(self : Element, value : T) -> Element {
  self.property(@protocol.MIN_HEIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's min_h helper.
pub fn[T : IntoValue] Style::min_h(self : Style, value : T) -> Style {
  self.property(@protocol.MIN_HEIGHT, "minHeight", IntoValue::into_value(value))
}

///|
/// Matches Rust's min_w helper.
pub fn[T : IntoValue] Element::min_w(self : Element, value : T) -> Element {
  self.property(@protocol.MIN_WIDTH, IntoValue::into_value(value))
}

///|
/// Matches Rust's min_w helper.
pub fn[T : IntoValue] Style::min_w(self : Style, value : T) -> Style {
  self.property(@protocol.MIN_WIDTH, "minWidth", IntoValue::into_value(value))
}

///|
/// Matches Rust's ml helper.
pub fn[T : IntoValue] Element::ml(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(value))
}

///|
/// Matches Rust's ml helper.
pub fn[T : IntoValue] Style::ml(self : Style, value : T) -> Style {
  self.property(
    @protocol.MARGIN_LEFT,
    "marginLeft",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's ml_0 helper.
pub fn Element::ml_0(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(0))
}

///|
/// Matches Rust's ml_0 helper.
pub fn Style::ml_0(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(0))
}

///|
/// Matches Rust's ml_1 helper.
pub fn Element::ml_1(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(4))
}

///|
/// Matches Rust's ml_1 helper.
pub fn Style::ml_1(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(4))
}

///|
/// Matches Rust's ml_10 helper.
pub fn Element::ml_10(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(40))
}

///|
/// Matches Rust's ml_10 helper.
pub fn Style::ml_10(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(40))
}

///|
/// Matches Rust's ml_12 helper.
pub fn Element::ml_12(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(48))
}

///|
/// Matches Rust's ml_12 helper.
pub fn Style::ml_12(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(48))
}

///|
/// Matches Rust's ml_16 helper.
pub fn Element::ml_16(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(64))
}

///|
/// Matches Rust's ml_16 helper.
pub fn Style::ml_16(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(64))
}

///|
/// Matches Rust's ml_2 helper.
pub fn Element::ml_2(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(8))
}

///|
/// Matches Rust's ml_2 helper.
pub fn Style::ml_2(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(8))
}

///|
/// Matches Rust's ml_20 helper.
pub fn Element::ml_20(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(80))
}

///|
/// Matches Rust's ml_20 helper.
pub fn Style::ml_20(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(80))
}

///|
/// Matches Rust's ml_24 helper.
pub fn Element::ml_24(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(96))
}

///|
/// Matches Rust's ml_24 helper.
pub fn Style::ml_24(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(96))
}

///|
/// Matches Rust's ml_3 helper.
pub fn Element::ml_3(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(12))
}

///|
/// Matches Rust's ml_3 helper.
pub fn Style::ml_3(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(12))
}

///|
/// Matches Rust's ml_32 helper.
pub fn Element::ml_32(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(128))
}

///|
/// Matches Rust's ml_32 helper.
pub fn Style::ml_32(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(128))
}

///|
/// Matches Rust's ml_4 helper.
pub fn Element::ml_4(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(16))
}

///|
/// Matches Rust's ml_4 helper.
pub fn Style::ml_4(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(16))
}

///|
/// Matches Rust's ml_5 helper.
pub fn Element::ml_5(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(20))
}

///|
/// Matches Rust's ml_5 helper.
pub fn Style::ml_5(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(20))
}

///|
/// Matches Rust's ml_6 helper.
pub fn Element::ml_6(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(24))
}

///|
/// Matches Rust's ml_6 helper.
pub fn Style::ml_6(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(24))
}

///|
/// Matches Rust's ml_8 helper.
pub fn Element::ml_8(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value(32))
}

///|
/// Matches Rust's ml_8 helper.
pub fn Style::ml_8(self : Style) -> Style {
  self.property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(32))
}

///|
/// Matches Rust's ml_auto helper.
pub fn Element::ml_auto(self : Element) -> Element {
  self.property(@protocol.MARGIN_LEFT, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's ml_auto helper.
pub fn Style::ml_auto(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_LEFT,
    "marginLeft",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's mr helper.
pub fn[T : IntoValue] Element::mr(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's mr helper.
pub fn[T : IntoValue] Style::mr(self : Style, value : T) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's mr_0 helper.
pub fn Element::mr_0(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(0))
}

///|
/// Matches Rust's mr_0 helper.
pub fn Style::mr_0(self : Style) -> Style {
  self.property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(0))
}

///|
/// Matches Rust's mr_1 helper.
pub fn Element::mr_1(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(4))
}

///|
/// Matches Rust's mr_1 helper.
pub fn Style::mr_1(self : Style) -> Style {
  self.property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(4))
}

///|
/// Matches Rust's mr_10 helper.
pub fn Element::mr_10(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(40))
}

///|
/// Matches Rust's mr_10 helper.
pub fn Style::mr_10(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(40),
  )
}

///|
/// Matches Rust's mr_12 helper.
pub fn Element::mr_12(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(48))
}

///|
/// Matches Rust's mr_12 helper.
pub fn Style::mr_12(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(48),
  )
}

///|
/// Matches Rust's mr_16 helper.
pub fn Element::mr_16(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(64))
}

///|
/// Matches Rust's mr_16 helper.
pub fn Style::mr_16(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(64),
  )
}

///|
/// Matches Rust's mr_2 helper.
pub fn Element::mr_2(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(8))
}

///|
/// Matches Rust's mr_2 helper.
pub fn Style::mr_2(self : Style) -> Style {
  self.property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(8))
}

///|
/// Matches Rust's mr_20 helper.
pub fn Element::mr_20(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(80))
}

///|
/// Matches Rust's mr_20 helper.
pub fn Style::mr_20(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(80),
  )
}

///|
/// Matches Rust's mr_24 helper.
pub fn Element::mr_24(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(96))
}

///|
/// Matches Rust's mr_24 helper.
pub fn Style::mr_24(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(96),
  )
}

///|
/// Matches Rust's mr_3 helper.
pub fn Element::mr_3(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(12))
}

///|
/// Matches Rust's mr_3 helper.
pub fn Style::mr_3(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(12),
  )
}

///|
/// Matches Rust's mr_32 helper.
pub fn Element::mr_32(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(128))
}

///|
/// Matches Rust's mr_32 helper.
pub fn Style::mr_32(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(128),
  )
}

///|
/// Matches Rust's mr_4 helper.
pub fn Element::mr_4(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(16))
}

///|
/// Matches Rust's mr_4 helper.
pub fn Style::mr_4(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(16),
  )
}

///|
/// Matches Rust's mr_5 helper.
pub fn Element::mr_5(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(20))
}

///|
/// Matches Rust's mr_5 helper.
pub fn Style::mr_5(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(20),
  )
}

///|
/// Matches Rust's mr_6 helper.
pub fn Element::mr_6(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(24))
}

///|
/// Matches Rust's mr_6 helper.
pub fn Style::mr_6(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(24),
  )
}

///|
/// Matches Rust's mr_8 helper.
pub fn Element::mr_8(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value(32))
}

///|
/// Matches Rust's mr_8 helper.
pub fn Style::mr_8(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value(32),
  )
}

///|
/// Matches Rust's mr_auto helper.
pub fn Element::mr_auto(self : Element) -> Element {
  self.property(@protocol.MARGIN_RIGHT, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's mr_auto helper.
pub fn Style::mr_auto(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's ms helper.
pub fn[T : IntoValue] Element::ms(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_START, IntoValue::into_value(value))
}

///|
/// Matches Rust's ms helper.
pub fn[T : IntoValue] Style::ms(self : Style, value : T) -> Style {
  self.property(
    @protocol.MARGIN_START,
    "marginStart",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's mt helper.
pub fn[T : IntoValue] Element::mt(self : Element, value : T) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(value))
}

///|
/// Matches Rust's mt helper.
pub fn[T : IntoValue] Style::mt(self : Style, value : T) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(value))
}

///|
/// Matches Rust's mt_0 helper.
pub fn Element::mt_0(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(0))
}

///|
/// Matches Rust's mt_0 helper.
pub fn Style::mt_0(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(0))
}

///|
/// Matches Rust's mt_1 helper.
pub fn Element::mt_1(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(4))
}

///|
/// Matches Rust's mt_1 helper.
pub fn Style::mt_1(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(4))
}

///|
/// Matches Rust's mt_10 helper.
pub fn Element::mt_10(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(40))
}

///|
/// Matches Rust's mt_10 helper.
pub fn Style::mt_10(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(40))
}

///|
/// Matches Rust's mt_12 helper.
pub fn Element::mt_12(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(48))
}

///|
/// Matches Rust's mt_12 helper.
pub fn Style::mt_12(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(48))
}

///|
/// Matches Rust's mt_16 helper.
pub fn Element::mt_16(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(64))
}

///|
/// Matches Rust's mt_16 helper.
pub fn Style::mt_16(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(64))
}

///|
/// Matches Rust's mt_2 helper.
pub fn Element::mt_2(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(8))
}

///|
/// Matches Rust's mt_2 helper.
pub fn Style::mt_2(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(8))
}

///|
/// Matches Rust's mt_20 helper.
pub fn Element::mt_20(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(80))
}

///|
/// Matches Rust's mt_20 helper.
pub fn Style::mt_20(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(80))
}

///|
/// Matches Rust's mt_24 helper.
pub fn Element::mt_24(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(96))
}

///|
/// Matches Rust's mt_24 helper.
pub fn Style::mt_24(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(96))
}

///|
/// Matches Rust's mt_3 helper.
pub fn Element::mt_3(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(12))
}

///|
/// Matches Rust's mt_3 helper.
pub fn Style::mt_3(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(12))
}

///|
/// Matches Rust's mt_32 helper.
pub fn Element::mt_32(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(128))
}

///|
/// Matches Rust's mt_32 helper.
pub fn Style::mt_32(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(128))
}

///|
/// Matches Rust's mt_4 helper.
pub fn Element::mt_4(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(16))
}

///|
/// Matches Rust's mt_4 helper.
pub fn Style::mt_4(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(16))
}

///|
/// Matches Rust's mt_5 helper.
pub fn Element::mt_5(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(20))
}

///|
/// Matches Rust's mt_5 helper.
pub fn Style::mt_5(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(20))
}

///|
/// Matches Rust's mt_6 helper.
pub fn Element::mt_6(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(24))
}

///|
/// Matches Rust's mt_6 helper.
pub fn Style::mt_6(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(24))
}

///|
/// Matches Rust's mt_8 helper.
pub fn Element::mt_8(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value(32))
}

///|
/// Matches Rust's mt_8 helper.
pub fn Style::mt_8(self : Style) -> Style {
  self.property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(32))
}

///|
/// Matches Rust's mt_auto helper.
pub fn Element::mt_auto(self : Element) -> Element {
  self.property(@protocol.MARGIN_TOP, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's mt_auto helper.
pub fn Style::mt_auto(self : Style) -> Style {
  self.property(
    @protocol.MARGIN_TOP,
    "marginTop",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's mx helper.
pub fn[T : IntoValue] Element::mx(self : Element, value : T) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(value))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's mx helper.
pub fn[T : IntoValue] Style::mx(self : Style, value : T) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(value))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(value))
}

///|
/// Matches Rust's mx_0 helper.
pub fn Element::mx_0(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(0))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(0))
}

///|
/// Matches Rust's mx_0 helper.
pub fn Style::mx_0(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(0))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(0))
}

///|
/// Matches Rust's mx_1 helper.
pub fn Element::mx_1(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(4))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(4))
}

///|
/// Matches Rust's mx_1 helper.
pub fn Style::mx_1(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(4))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(4))
}

///|
/// Matches Rust's mx_10 helper.
pub fn Element::mx_10(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(40))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(40))
}

///|
/// Matches Rust's mx_10 helper.
pub fn Style::mx_10(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(40))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(40))
}

///|
/// Matches Rust's mx_12 helper.
pub fn Element::mx_12(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(48))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(48))
}

///|
/// Matches Rust's mx_12 helper.
pub fn Style::mx_12(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(48))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(48))
}

///|
/// Matches Rust's mx_16 helper.
pub fn Element::mx_16(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(64))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(64))
}

///|
/// Matches Rust's mx_16 helper.
pub fn Style::mx_16(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(64))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(64))
}

///|
/// Matches Rust's mx_2 helper.
pub fn Element::mx_2(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(8))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(8))
}

///|
/// Matches Rust's mx_2 helper.
pub fn Style::mx_2(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(8))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(8))
}

///|
/// Matches Rust's mx_20 helper.
pub fn Element::mx_20(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(80))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(80))
}

///|
/// Matches Rust's mx_20 helper.
pub fn Style::mx_20(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(80))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(80))
}

///|
/// Matches Rust's mx_24 helper.
pub fn Element::mx_24(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(96))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(96))
}

///|
/// Matches Rust's mx_24 helper.
pub fn Style::mx_24(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(96))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(96))
}

///|
/// Matches Rust's mx_3 helper.
pub fn Element::mx_3(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(12))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(12))
}

///|
/// Matches Rust's mx_3 helper.
pub fn Style::mx_3(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(12))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(12))
}

///|
/// Matches Rust's mx_32 helper.
pub fn Element::mx_32(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(128))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(128))
}

///|
/// Matches Rust's mx_32 helper.
pub fn Style::mx_32(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(128))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(128))
}

///|
/// Matches Rust's mx_4 helper.
pub fn Element::mx_4(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(16))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(16))
}

///|
/// Matches Rust's mx_4 helper.
pub fn Style::mx_4(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(16))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(16))
}

///|
/// Matches Rust's mx_5 helper.
pub fn Element::mx_5(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(20))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(20))
}

///|
/// Matches Rust's mx_5 helper.
pub fn Style::mx_5(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(20))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(20))
}

///|
/// Matches Rust's mx_6 helper.
pub fn Element::mx_6(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(24))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(24))
}

///|
/// Matches Rust's mx_6 helper.
pub fn Style::mx_6(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(24))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(24))
}

///|
/// Matches Rust's mx_8 helper.
pub fn Element::mx_8(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value(32))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value(32))
}

///|
/// Matches Rust's mx_8 helper.
pub fn Style::mx_8(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value(32))
  .property(@protocol.MARGIN_RIGHT, "marginRight", IntoValue::into_value(32))
}

///|
/// Matches Rust's mx_auto helper.
pub fn Element::mx_auto(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_LEFT, IntoValue::into_value("auto"))
  .property(@protocol.MARGIN_RIGHT, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's mx_auto helper.
pub fn Style::mx_auto(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_LEFT, "marginLeft", IntoValue::into_value("auto"))
  .property(
    @protocol.MARGIN_RIGHT,
    "marginRight",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's my helper.
pub fn[T : IntoValue] Element::my(self : Element, value : T) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(value))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(value))
}

///|
/// Matches Rust's my helper.
pub fn[T : IntoValue] Style::my(self : Style, value : T) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(value))
  .property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's my_0 helper.
pub fn Element::my_0(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(0))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(0))
}

///|
/// Matches Rust's my_0 helper.
pub fn Style::my_0(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(0))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(0))
}

///|
/// Matches Rust's my_1 helper.
pub fn Element::my_1(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(4))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(4))
}

///|
/// Matches Rust's my_1 helper.
pub fn Style::my_1(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(4))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(4))
}

///|
/// Matches Rust's my_10 helper.
pub fn Element::my_10(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(40))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(40))
}

///|
/// Matches Rust's my_10 helper.
pub fn Style::my_10(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(40))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(40))
}

///|
/// Matches Rust's my_12 helper.
pub fn Element::my_12(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(48))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(48))
}

///|
/// Matches Rust's my_12 helper.
pub fn Style::my_12(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(48))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(48))
}

///|
/// Matches Rust's my_16 helper.
pub fn Element::my_16(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(64))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(64))
}

///|
/// Matches Rust's my_16 helper.
pub fn Style::my_16(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(64))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(64))
}

///|
/// Matches Rust's my_2 helper.
pub fn Element::my_2(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(8))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(8))
}

///|
/// Matches Rust's my_2 helper.
pub fn Style::my_2(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(8))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(8))
}

///|
/// Matches Rust's my_20 helper.
pub fn Element::my_20(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(80))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(80))
}

///|
/// Matches Rust's my_20 helper.
pub fn Style::my_20(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(80))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(80))
}

///|
/// Matches Rust's my_24 helper.
pub fn Element::my_24(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(96))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(96))
}

///|
/// Matches Rust's my_24 helper.
pub fn Style::my_24(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(96))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(96))
}

///|
/// Matches Rust's my_3 helper.
pub fn Element::my_3(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(12))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(12))
}

///|
/// Matches Rust's my_3 helper.
pub fn Style::my_3(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(12))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(12))
}

///|
/// Matches Rust's my_32 helper.
pub fn Element::my_32(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(128))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(128))
}

///|
/// Matches Rust's my_32 helper.
pub fn Style::my_32(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(128))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(128))
}

///|
/// Matches Rust's my_4 helper.
pub fn Element::my_4(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(16))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(16))
}

///|
/// Matches Rust's my_4 helper.
pub fn Style::my_4(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(16))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(16))
}

///|
/// Matches Rust's my_5 helper.
pub fn Element::my_5(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(20))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(20))
}

///|
/// Matches Rust's my_5 helper.
pub fn Style::my_5(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(20))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(20))
}

///|
/// Matches Rust's my_6 helper.
pub fn Element::my_6(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(24))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(24))
}

///|
/// Matches Rust's my_6 helper.
pub fn Style::my_6(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(24))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(24))
}

///|
/// Matches Rust's my_8 helper.
pub fn Element::my_8(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value(32))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value(32))
}

///|
/// Matches Rust's my_8 helper.
pub fn Style::my_8(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value(32))
  .property(@protocol.MARGIN_BOTTOM, "marginBottom", IntoValue::into_value(32))
}

///|
/// Matches Rust's my_auto helper.
pub fn Element::my_auto(self : Element) -> Element {
  self
  .property(@protocol.MARGIN_TOP, IntoValue::into_value("auto"))
  .property(@protocol.MARGIN_BOTTOM, IntoValue::into_value("auto"))
}

///|
/// Matches Rust's my_auto helper.
pub fn Style::my_auto(self : Style) -> Style {
  self
  .property(@protocol.MARGIN_TOP, "marginTop", IntoValue::into_value("auto"))
  .property(
    @protocol.MARGIN_BOTTOM,
    "marginBottom",
    IntoValue::into_value("auto"),
  )
}

///|
/// Matches Rust's overflow_hidden helper.
pub fn Element::overflow_hidden(self : Element) -> Element {
  self
  .property(@protocol.OVERFLOW_X, IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, IntoValue::into_value("hidden"))
}

///|
/// Matches Rust's overflow_hidden helper.
pub fn Style::overflow_hidden(self : Style) -> Style {
  self
  .property(@protocol.OVERFLOW_X, "overflowX", IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, "overflowY", IntoValue::into_value("hidden"))
}

///|
/// Matches Rust's overflow_scroll helper.
pub fn Element::overflow_scroll(self : Element) -> Element {
  self
  .property(@protocol.OVERFLOW_X, IntoValue::into_value("scroll"))
  .property(@protocol.OVERFLOW_Y, IntoValue::into_value("scroll"))
}

///|
/// Matches Rust's overflow_scroll helper.
pub fn Style::overflow_scroll(self : Style) -> Style {
  self
  .property(@protocol.OVERFLOW_X, "overflowX", IntoValue::into_value("scroll"))
  .property(@protocol.OVERFLOW_Y, "overflowY", IntoValue::into_value("scroll"))
}

///|
/// Matches Rust's overflow_x_scroll helper.
pub fn Element::overflow_x_scroll(self : Element) -> Element {
  self
  .property(@protocol.OVERFLOW_X, IntoValue::into_value("scroll"))
  .property(@protocol.OVERFLOW_Y, IntoValue::into_value("hidden"))
}

///|
/// Matches Rust's overflow_x_scroll helper.
pub fn Style::overflow_x_scroll(self : Style) -> Style {
  self
  .property(@protocol.OVERFLOW_X, "overflowX", IntoValue::into_value("scroll"))
  .property(@protocol.OVERFLOW_Y, "overflowY", IntoValue::into_value("hidden"))
}

///|
/// Matches Rust's overflow_y_scroll helper.
pub fn Element::overflow_y_scroll(self : Element) -> Element {
  self
  .property(@protocol.OVERFLOW_X, IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, IntoValue::into_value("scroll"))
}

///|
/// Matches Rust's overflow_y_scroll helper.
pub fn Style::overflow_y_scroll(self : Style) -> Style {
  self
  .property(@protocol.OVERFLOW_X, "overflowX", IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, "overflowY", IntoValue::into_value("scroll"))
}

///|
/// Matches Rust's p helper.
pub fn[T : IntoValue] Element::p(self : Element, value : T) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(value))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(value))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(value))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(value))
}

///|
/// Matches Rust's p helper.
pub fn[T : IntoValue] Style::p(self : Style, value : T) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(value))
  .property(
    @protocol.PADDING_RIGHT,
    "paddingRight",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(value),
  )
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(value))
}

///|
/// Matches Rust's p_1 helper.
pub fn Element::p_1(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(4))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(4))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(4))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(4))
}

///|
/// Matches Rust's p_1 helper.
pub fn Style::p_1(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(4))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(4))
  .property(@protocol.PADDING_BOTTOM, "paddingBottom", IntoValue::into_value(4))
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(4))
}

///|
/// Matches Rust's p_2 helper.
pub fn Element::p_2(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(8))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(8))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(8))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(8))
}

///|
/// Matches Rust's p_2 helper.
pub fn Style::p_2(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(8))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(8))
  .property(@protocol.PADDING_BOTTOM, "paddingBottom", IntoValue::into_value(8))
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(8))
}

///|
/// Matches Rust's p_3 helper.
pub fn Element::p_3(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(12))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(12))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(12))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(12))
}

///|
/// Matches Rust's p_3 helper.
pub fn Style::p_3(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(12))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(12))
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(12),
  )
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(12))
}

///|
/// Matches Rust's p_4 helper.
pub fn Element::p_4(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(16))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(16))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(16))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(16))
}

///|
/// Matches Rust's p_4 helper.
pub fn Style::p_4(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(16))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(16))
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(16),
  )
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(16))
}

///|
/// Matches Rust's p_5 helper.
pub fn Element::p_5(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(20))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(20))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(20))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(20))
}

///|
/// Matches Rust's p_5 helper.
pub fn Style::p_5(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(20))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(20))
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(20),
  )
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(20))
}

///|
/// Matches Rust's p_6 helper.
pub fn Element::p_6(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(24))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(24))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(24))
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(24))
}

///|
/// Matches Rust's p_6 helper.
pub fn Style::p_6(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(24))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(24))
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(24),
  )
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(24))
}

///|
/// Matches Rust's pe helper.
pub fn[T : IntoValue] Element::pe(self : Element, value : T) -> Element {
  self.property(@protocol.PADDING_END, IntoValue::into_value(value))
}

///|
/// Matches Rust's pe helper.
pub fn[T : IntoValue] Style::pe(self : Style, value : T) -> Style {
  self.property(
    @protocol.PADDING_END,
    "paddingEnd",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's ps helper.
pub fn[T : IntoValue] Element::ps(self : Element, value : T) -> Element {
  self.property(@protocol.PADDING_START, IntoValue::into_value(value))
}

///|
/// Matches Rust's ps helper.
pub fn[T : IntoValue] Style::ps(self : Style, value : T) -> Style {
  self.property(
    @protocol.PADDING_START,
    "paddingStart",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's px helper.
pub fn[T : IntoValue] Element::px(self : Element, value : T) -> Element {
  self
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(value))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's px helper.
pub fn[T : IntoValue] Style::px(self : Style, value : T) -> Style {
  self
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(value))
  .property(
    @protocol.PADDING_RIGHT,
    "paddingRight",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's px_2 helper.
pub fn Element::px_2(self : Element) -> Element {
  self
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(8))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(8))
}

///|
/// Matches Rust's px_2 helper.
pub fn Style::px_2(self : Style) -> Style {
  self
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(8))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(8))
}

///|
/// Matches Rust's px_3 helper.
pub fn Element::px_3(self : Element) -> Element {
  self
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(12))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(12))
}

///|
/// Matches Rust's px_3 helper.
pub fn Style::px_3(self : Style) -> Style {
  self
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(12))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(12))
}

///|
/// Matches Rust's px_4 helper.
pub fn Element::px_4(self : Element) -> Element {
  self
  .property(@protocol.PADDING_LEFT, IntoValue::into_value(16))
  .property(@protocol.PADDING_RIGHT, IntoValue::into_value(16))
}

///|
/// Matches Rust's px_4 helper.
pub fn Style::px_4(self : Style) -> Style {
  self
  .property(@protocol.PADDING_LEFT, "paddingLeft", IntoValue::into_value(16))
  .property(@protocol.PADDING_RIGHT, "paddingRight", IntoValue::into_value(16))
}

///|
/// Matches Rust's py helper.
pub fn[T : IntoValue] Element::py(self : Element, value : T) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(value))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(value))
}

///|
/// Matches Rust's py helper.
pub fn[T : IntoValue] Style::py(self : Style, value : T) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(value))
  .property(
    @protocol.PADDING_BOTTOM,
    "paddingBottom",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's py_1 helper.
pub fn Element::py_1(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(4))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(4))
}

///|
/// Matches Rust's py_1 helper.
pub fn Style::py_1(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(4))
  .property(@protocol.PADDING_BOTTOM, "paddingBottom", IntoValue::into_value(4))
}

///|
/// Matches Rust's py_2 helper.
pub fn Element::py_2(self : Element) -> Element {
  self
  .property(@protocol.PADDING_TOP, IntoValue::into_value(8))
  .property(@protocol.PADDING_BOTTOM, IntoValue::into_value(8))
}

///|
/// Matches Rust's py_2 helper.
pub fn Style::py_2(self : Style) -> Style {
  self
  .property(@protocol.PADDING_TOP, "paddingTop", IntoValue::into_value(8))
  .property(@protocol.PADDING_BOTTOM, "paddingBottom", IntoValue::into_value(8))
}

///|
/// Matches Rust's relative helper.
pub fn Element::relative(self : Element) -> Element {
  self.property(@protocol.POSITION, IntoValue::into_value("relative"))
}

///|
/// Matches Rust's relative helper.
pub fn Style::relative(self : Style) -> Style {
  self.property(
    @protocol.POSITION,
    "position",
    IntoValue::into_value("relative"),
  )
}

///|
/// Matches Rust's rounded helper.
pub fn[T : IntoValue] Element::rounded(self : Element, value : T) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(value))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded helper.
pub fn[T : IntoValue] Style::rounded(self : Style, value : T) -> Style {
  self
  .property(
    @protocol.BORDER_RADIUS,
    "borderRadius",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_2xl helper.
pub fn Element::rounded_2xl(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(16))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_2xl helper.
pub fn Style::rounded_2xl(self : Style) -> Style {
  self
  .property(@protocol.BORDER_RADIUS, "borderRadius", IntoValue::into_value(16))
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_b helper.
pub fn[T : IntoValue] Element::rounded_b(self : Element, value : T) -> Element {
  self
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, IntoValue::into_value(value))
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_b helper.
pub fn[T : IntoValue] Style::rounded_b(self : Style, value : T) -> Style {
  self
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_bl helper.
pub fn[T : IntoValue] Element::rounded_bl(self : Element, value : T) -> Element {
  self.property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_bl helper.
pub fn[T : IntoValue] Style::rounded_bl(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_br helper.
pub fn[T : IntoValue] Element::rounded_br(self : Element, value : T) -> Element {
  self.property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_br helper.
pub fn[T : IntoValue] Style::rounded_br(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_full helper.
pub fn Element::rounded_full(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(4096))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_full helper.
pub fn Style::rounded_full(self : Style) -> Style {
  self
  .property(
    @protocol.BORDER_RADIUS,
    "borderRadius",
    IntoValue::into_value(4096),
  )
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_l helper.
pub fn[T : IntoValue] Element::rounded_l(self : Element, value : T) -> Element {
  self
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, IntoValue::into_value(value))
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_l helper.
pub fn[T : IntoValue] Style::rounded_l(self : Style, value : T) -> Style {
  self
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_lg helper.
pub fn Element::rounded_lg(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(8))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_lg helper.
pub fn Style::rounded_lg(self : Style) -> Style {
  self
  .property(@protocol.BORDER_RADIUS, "borderRadius", IntoValue::into_value(8))
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_md helper.
pub fn Element::rounded_md(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(6))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_md helper.
pub fn Style::rounded_md(self : Style) -> Style {
  self
  .property(@protocol.BORDER_RADIUS, "borderRadius", IntoValue::into_value(6))
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_r helper.
pub fn[T : IntoValue] Element::rounded_r(self : Element, value : T) -> Element {
  self
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, IntoValue::into_value(value))
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_r helper.
pub fn[T : IntoValue] Style::rounded_r(self : Style, value : T) -> Style {
  self
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_sm helper.
pub fn Element::rounded_sm(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(4))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_sm helper.
pub fn Style::rounded_sm(self : Style) -> Style {
  self
  .property(@protocol.BORDER_RADIUS, "borderRadius", IntoValue::into_value(4))
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's rounded_t helper.
pub fn[T : IntoValue] Element::rounded_t(self : Element, value : T) -> Element {
  self
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, IntoValue::into_value(value))
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_t helper.
pub fn[T : IntoValue] Style::rounded_t(self : Style, value : T) -> Style {
  self
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    IntoValue::into_value(value),
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_tl helper.
pub fn[T : IntoValue] Element::rounded_tl(self : Element, value : T) -> Element {
  self.property(@protocol.BORDER_TOP_LEFT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_tl helper.
pub fn[T : IntoValue] Style::rounded_tl(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_tr helper.
pub fn[T : IntoValue] Element::rounded_tr(self : Element, value : T) -> Element {
  self.property(@protocol.BORDER_TOP_RIGHT_RADIUS, IntoValue::into_value(value))
}

///|
/// Matches Rust's rounded_tr helper.
pub fn[T : IntoValue] Style::rounded_tr(self : Style, value : T) -> Style {
  self.property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's rounded_xl helper.
pub fn Element::rounded_xl(self : Element) -> Element {
  self
  .property(@protocol.BORDER_RADIUS, IntoValue::into_value(12))
  .property(@protocol.BORDER_TOP_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_TOP_RIGHT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_LEFT_RADIUS, @protocol.Clear)
  .property(@protocol.BORDER_BOTTOM_RIGHT_RADIUS, @protocol.Clear)
}

///|
/// Matches Rust's rounded_xl helper.
pub fn Style::rounded_xl(self : Style) -> Style {
  self
  .property(@protocol.BORDER_RADIUS, "borderRadius", IntoValue::into_value(12))
  .property(
    @protocol.BORDER_TOP_LEFT_RADIUS,
    "borderTopLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_TOP_RIGHT_RADIUS,
    "borderTopRightRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_LEFT_RADIUS,
    "borderBottomLeftRadius",
    @protocol.Clear,
  )
  .property(
    @protocol.BORDER_BOTTOM_RIGHT_RADIUS,
    "borderBottomRightRadius",
    @protocol.Clear,
  )
}

///|
/// Matches Rust's row_end helper.
pub fn[T : IntoValue] Element::row_end(self : Element, value : T) -> Element {
  self.property(@protocol.GRID_ROW_END, IntoValue::into_value(value))
}

///|
/// Matches Rust's row_end helper.
pub fn[T : IntoValue] Style::row_end(self : Style, value : T) -> Style {
  self.property(
    @protocol.GRID_ROW_END,
    "gridRowEnd",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's row_end_auto helper.
pub fn Element::row_end_auto(self : Element) -> Element {
  self.property(@protocol.GRID_ROW_END, IntoValue::into_value(0))
}

///|
/// Matches Rust's row_end_auto helper.
pub fn Style::row_end_auto(self : Style) -> Style {
  self.property(@protocol.GRID_ROW_END, "gridRowEnd", IntoValue::into_value(0))
}

///|
/// Matches Rust's row_span helper.
pub fn[T : IntoValue] Element::row_span(self : Element, value : T) -> Element {
  self
  .property(@protocol.GRID_ROW_START, @protocol.Clear)
  .property(@protocol.GRID_ROW_END, @protocol.Clear)
  .property(@protocol.GRID_ROW_SPAN, IntoValue::into_value(value))
}

///|
/// Matches Rust's row_span helper.
pub fn[T : IntoValue] Style::row_span(self : Style, value : T) -> Style {
  self
  .property(@protocol.GRID_ROW_START, "gridRowStart", @protocol.Clear)
  .property(@protocol.GRID_ROW_END, "gridRowEnd", @protocol.Clear)
  .property(
    @protocol.GRID_ROW_SPAN,
    "gridRowSpan",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's row_span_full helper.
pub fn Element::row_span_full(self : Element) -> Element {
  self
  .property(@protocol.GRID_ROW_SPAN, @protocol.Clear)
  .property(@protocol.GRID_ROW_START, IntoValue::into_value(1))
  .property(@protocol.GRID_ROW_END, IntoValue::into_value(-1))
}

///|
/// Matches Rust's row_span_full helper.
pub fn Style::row_span_full(self : Style) -> Style {
  self
  .property(@protocol.GRID_ROW_SPAN, "gridRowSpan", @protocol.Clear)
  .property(@protocol.GRID_ROW_START, "gridRowStart", IntoValue::into_value(1))
  .property(@protocol.GRID_ROW_END, "gridRowEnd", IntoValue::into_value(-1))
}

///|
/// Matches Rust's row_start helper.
pub fn[T : IntoValue] Element::row_start(self : Element, value : T) -> Element {
  self.property(@protocol.GRID_ROW_START, IntoValue::into_value(value))
}

///|
/// Matches Rust's row_start helper.
pub fn[T : IntoValue] Style::row_start(self : Style, value : T) -> Style {
  self.property(
    @protocol.GRID_ROW_START,
    "gridRowStart",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's row_start_auto helper.
pub fn Element::row_start_auto(self : Element) -> Element {
  self.property(@protocol.GRID_ROW_START, IntoValue::into_value(0))
}

///|
/// Matches Rust's row_start_auto helper.
pub fn Style::row_start_auto(self : Style) -> Style {
  self.property(
    @protocol.GRID_ROW_START,
    "gridRowStart",
    IntoValue::into_value(0),
  )
}

///|
/// Matches Rust's rtl helper.
pub fn Element::rtl(self : Element) -> Element {
  self.property(@protocol.DIRECTION, IntoValue::into_value("rtl"))
}

///|
/// Matches Rust's rtl helper.
pub fn Style::rtl(self : Style) -> Style {
  self.property(@protocol.DIRECTION, "direction", IntoValue::into_value("rtl"))
}

///|
/// Matches Rust's selectable helper.
pub fn Element::selectable(self : Element) -> Element {
  self.property(@protocol.USER_SELECT, IntoValue::into_value("text"))
}

///|
/// Matches Rust's selectable helper.
pub fn Style::selectable(self : Style) -> Style {
  self.property(
    @protocol.USER_SELECT,
    "userSelect",
    IntoValue::into_value("text"),
  )
}

///|
/// Matches Rust's self_baseline helper.
pub fn Element::self_baseline(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("baseline"))
}

///|
/// Matches Rust's self_baseline helper.
pub fn Style::self_baseline(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("baseline"),
  )
}

///|
/// Matches Rust's self_center helper.
pub fn Element::self_center(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("center"))
}

///|
/// Matches Rust's self_center helper.
pub fn Style::self_center(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("center"),
  )
}

///|
/// Matches Rust's self_end helper.
pub fn Element::self_end(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("end"))
}

///|
/// Matches Rust's self_end helper.
pub fn Style::self_end(self : Style) -> Style {
  self.property(@protocol.ALIGN_SELF, "alignSelf", IntoValue::into_value("end"))
}

///|
/// Matches Rust's self_flex_end helper.
pub fn Element::self_flex_end(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("flex-end"))
}

///|
/// Matches Rust's self_flex_end helper.
pub fn Style::self_flex_end(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("flex-end"),
  )
}

///|
/// Matches Rust's self_flex_start helper.
pub fn Element::self_flex_start(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("flex-start"))
}

///|
/// Matches Rust's self_flex_start helper.
pub fn Style::self_flex_start(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("flex-start"),
  )
}

///|
/// Matches Rust's self_start helper.
pub fn Element::self_start(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("start"))
}

///|
/// Matches Rust's self_start helper.
pub fn Style::self_start(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("start"),
  )
}

///|
/// Matches Rust's self_stretch helper.
pub fn Element::self_stretch(self : Element) -> Element {
  self.property(@protocol.ALIGN_SELF, IntoValue::into_value("stretch"))
}

///|
/// Matches Rust's self_stretch helper.
pub fn Style::self_stretch(self : Style) -> Style {
  self.property(
    @protocol.ALIGN_SELF,
    "alignSelf",
    IntoValue::into_value("stretch"),
  )
}

///|
/// Matches Rust's size helper.
pub fn[T : IntoValue] Element::size(
  self : Element,
  width : T,
  height : T,
) -> Element {
  self
  .property(@protocol.WIDTH, IntoValue::into_value(width))
  .property(@protocol.HEIGHT, IntoValue::into_value(height))
}

///|
/// Matches Rust's size helper.
pub fn[T : IntoValue] Style::size(self : Style, width : T, height : T) -> Style {
  self
  .property(@protocol.WIDTH, "width", IntoValue::into_value(width))
  .property(@protocol.HEIGHT, "height", IntoValue::into_value(height))
}

///|
/// Matches Rust's size_full helper.
pub fn Element::size_full(self : Element) -> Element {
  self
  .property(@protocol.WIDTH, IntoValue::into_value("100%"))
  .property(@protocol.HEIGHT, IntoValue::into_value("100%"))
}

///|
/// Matches Rust's size_full helper.
pub fn Style::size_full(self : Style) -> Style {
  self
  .property(@protocol.WIDTH, "width", IntoValue::into_value("100%"))
  .property(@protocol.HEIGHT, "height", IntoValue::into_value("100%"))
}

///|
/// Matches Rust's snap_align helper.
pub fn Element::snap_align(self : Element, value : String) -> Element {
  self.property(@protocol.SCROLL_SNAP_ALIGN, IntoValue::into_value(value))
}

///|
/// Matches Rust's snap_align helper.
pub fn Style::snap_align(self : Style, value : String) -> Style {
  self.property(
    @protocol.SCROLL_SNAP_ALIGN,
    "scrollSnapAlign",
    IntoValue::into_value(value),
  )
}

///|
/// Matches Rust's snap_stop_always helper.
pub fn Element::snap_stop_always(self : Element) -> Element {
  self.property(@protocol.SCROLL_SNAP_STOP, IntoValue::into_value("always"))
}

///|
/// Matches Rust's snap_stop_always helper.
pub fn Style::snap_stop_always(self : Style) -> Style {
  self.property(
    @protocol.SCROLL_SNAP_STOP,
    "scrollSnapStop",
    IntoValue::into_value("always"),
  )
}

///|
/// Matches Rust's sticky helper.
pub fn Element::sticky(self : Element) -> Element {
  self.property(@protocol.POSITION, IntoValue::into_value("sticky"))
}

///|
/// Matches Rust's sticky helper.
pub fn Style::sticky(self : Style) -> Style {
  self.property(@protocol.POSITION, "position", IntoValue::into_value("sticky"))
}

///|
/// Matches Rust's sticky_bottom helper.
pub fn[T : IntoValue] Element::sticky_bottom(
  self : Element,
  value : T,
) -> Element {
  self
  .property(@protocol.POSITION, IntoValue::into_value("sticky"))
  .property(@protocol.BOTTOM, IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_bottom helper.
pub fn[T : IntoValue] Style::sticky_bottom(self : Style, value : T) -> Style {
  self
  .property(@protocol.POSITION, "position", IntoValue::into_value("sticky"))
  .property(@protocol.BOTTOM, "bottom", IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_left helper.
pub fn[T : IntoValue] Element::sticky_left(
  self : Element,
  value : T,
) -> Element {
  self
  .property(@protocol.POSITION, IntoValue::into_value("sticky"))
  .property(@protocol.LEFT, IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_left helper.
pub fn[T : IntoValue] Style::sticky_left(self : Style, value : T) -> Style {
  self
  .property(@protocol.POSITION, "position", IntoValue::into_value("sticky"))
  .property(@protocol.LEFT, "left", IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_right helper.
pub fn[T : IntoValue] Element::sticky_right(
  self : Element,
  value : T,
) -> Element {
  self
  .property(@protocol.POSITION, IntoValue::into_value("sticky"))
  .property(@protocol.RIGHT, IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_right helper.
pub fn[T : IntoValue] Style::sticky_right(self : Style, value : T) -> Style {
  self
  .property(@protocol.POSITION, "position", IntoValue::into_value("sticky"))
  .property(@protocol.RIGHT, "right", IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_top helper.
pub fn[T : IntoValue] Element::sticky_top(self : Element, value : T) -> Element {
  self
  .property(@protocol.POSITION, IntoValue::into_value("sticky"))
  .property(@protocol.TOP, IntoValue::into_value(value))
}

///|
/// Matches Rust's sticky_top helper.
pub fn[T : IntoValue] Style::sticky_top(self : Style, value : T) -> Style {
  self
  .property(@protocol.POSITION, "position", IntoValue::into_value("sticky"))
  .property(@protocol.TOP, "top", IntoValue::into_value(value))
}

///|
/// Matches Rust's text_2xl helper.
pub fn Element::text_2xl(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(24))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(32))
}

///|
/// Matches Rust's text_2xl helper.
pub fn Style::text_2xl(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(24))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(32))
}

///|
/// Matches Rust's text_3xl helper.
pub fn Element::text_3xl(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(30))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(36))
}

///|
/// Matches Rust's text_3xl helper.
pub fn Style::text_3xl(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(30))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(36))
}

///|
/// Matches Rust's text_base helper.
pub fn Element::text_base(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(16))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(24))
}

///|
/// Matches Rust's text_base helper.
pub fn Style::text_base(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(16))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(24))
}

///|
/// Matches Rust's text_center helper.
pub fn Element::text_center(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("center"))
}

///|
/// Matches Rust's text_center helper.
pub fn Style::text_center(self : Style) -> Style {
  self.property(
    @protocol.TEXT_ALIGN,
    "textAlign",
    IntoValue::into_value("center"),
  )
}

///|
/// Matches Rust's text_ellipsis helper.
pub fn Element::text_ellipsis(self : Element) -> Element {
  self.property(@protocol.TEXT_OVERFLOW, IntoValue::into_value("ellipsis"))
}

///|
/// Matches Rust's text_ellipsis helper.
pub fn Style::text_ellipsis(self : Style) -> Style {
  self.property(
    @protocol.TEXT_OVERFLOW,
    "textOverflow",
    IntoValue::into_value("ellipsis"),
  )
}

///|
/// Matches Rust's text_ellipsis_middle helper.
pub fn Element::text_ellipsis_middle(self : Element) -> Element {
  self.property(
    @protocol.TEXT_OVERFLOW,
    IntoValue::into_value("ellipsis-middle"),
  )
}

///|
/// Matches Rust's text_ellipsis_middle helper.
pub fn Style::text_ellipsis_middle(self : Style) -> Style {
  self.property(
    @protocol.TEXT_OVERFLOW,
    "textOverflow",
    IntoValue::into_value("ellipsis-middle"),
  )
}

///|
/// Matches Rust's text_ellipsis_start helper.
pub fn Element::text_ellipsis_start(self : Element) -> Element {
  self.property(
    @protocol.TEXT_OVERFLOW,
    IntoValue::into_value("ellipsis-start"),
  )
}

///|
/// Matches Rust's text_ellipsis_start helper.
pub fn Style::text_ellipsis_start(self : Style) -> Style {
  self.property(
    @protocol.TEXT_OVERFLOW,
    "textOverflow",
    IntoValue::into_value("ellipsis-start"),
  )
}

///|
/// Matches Rust's text_end helper.
pub fn Element::text_end(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("end"))
}

///|
/// Matches Rust's text_end helper.
pub fn Style::text_end(self : Style) -> Style {
  self.property(@protocol.TEXT_ALIGN, "textAlign", IntoValue::into_value("end"))
}

///|
/// Matches Rust's text_justify helper.
pub fn Element::text_justify(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("justify"))
}

///|
/// Matches Rust's text_justify helper.
pub fn Style::text_justify(self : Style) -> Style {
  self.property(
    @protocol.TEXT_ALIGN,
    "textAlign",
    IntoValue::into_value("justify"),
  )
}

///|
/// Matches Rust's text_left helper.
pub fn Element::text_left(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("left"))
}

///|
/// Matches Rust's text_left helper.
pub fn Style::text_left(self : Style) -> Style {
  self.property(
    @protocol.TEXT_ALIGN,
    "textAlign",
    IntoValue::into_value("left"),
  )
}

///|
/// Matches Rust's text_lg helper.
pub fn Element::text_lg(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(18))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(26))
}

///|
/// Matches Rust's text_lg helper.
pub fn Style::text_lg(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(18))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(26))
}

///|
/// Matches Rust's text_right helper.
pub fn Element::text_right(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("right"))
}

///|
/// Matches Rust's text_right helper.
pub fn Style::text_right(self : Style) -> Style {
  self.property(
    @protocol.TEXT_ALIGN,
    "textAlign",
    IntoValue::into_value("right"),
  )
}

///|
/// Matches Rust's text_sm helper.
pub fn Element::text_sm(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(14))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(20))
}

///|
/// Matches Rust's text_sm helper.
pub fn Style::text_sm(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(14))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(20))
}

///|
/// Matches Rust's text_start helper.
pub fn Element::text_start(self : Element) -> Element {
  self.property(@protocol.TEXT_ALIGN, IntoValue::into_value("start"))
}

///|
/// Matches Rust's text_start helper.
pub fn Style::text_start(self : Style) -> Style {
  self.property(
    @protocol.TEXT_ALIGN,
    "textAlign",
    IntoValue::into_value("start"),
  )
}

///|
/// Matches Rust's text_xl helper.
pub fn Element::text_xl(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(20))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(28))
}

///|
/// Matches Rust's text_xl helper.
pub fn Style::text_xl(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(20))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(28))
}

///|
/// Matches Rust's text_xs helper.
pub fn Element::text_xs(self : Element) -> Element {
  self
  .property(@protocol.FONT_SIZE, IntoValue::into_value(12))
  .property(@protocol.LINE_HEIGHT, IntoValue::into_value(16))
}

///|
/// Matches Rust's text_xs helper.
pub fn Style::text_xs(self : Style) -> Style {
  self
  .property(@protocol.FONT_SIZE, "fontSize", IntoValue::into_value(12))
  .property(@protocol.LINE_HEIGHT, "lineHeight", IntoValue::into_value(16))
}

///|
/// Matches Rust's truncate helper.
pub fn Element::truncate(self : Element) -> Element {
  self
  .property(@protocol.OVERFLOW_X, IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, IntoValue::into_value("hidden"))
  .property(@protocol.WHITE_SPACE, IntoValue::into_value("nowrap"))
  .property(@protocol.TEXT_OVERFLOW, IntoValue::into_value("ellipsis"))
}

///|
/// Matches Rust's truncate helper.
pub fn Style::truncate(self : Style) -> Style {
  self
  .property(@protocol.OVERFLOW_X, "overflowX", IntoValue::into_value("hidden"))
  .property(@protocol.OVERFLOW_Y, "overflowY", IntoValue::into_value("hidden"))
  .property(
    @protocol.WHITE_SPACE,
    "whiteSpace",
    IntoValue::into_value("nowrap"),
  )
  .property(
    @protocol.TEXT_OVERFLOW,
    "textOverflow",
    IntoValue::into_value("ellipsis"),
  )
}

///|
/// Matches Rust's user_select_none helper.
pub fn Element::user_select_none(self : Element) -> Element {
  self.property(@protocol.USER_SELECT, IntoValue::into_value("none"))
}

///|
/// Matches Rust's user_select_none helper.
pub fn Style::user_select_none(self : Style) -> Style {
  self.property(
    @protocol.USER_SELECT,
    "userSelect",
    IntoValue::into_value("none"),
  )
}

///|
/// Matches Rust's user_select_text helper.
pub fn Element::user_select_text(self : Element) -> Element {
  self.property(@protocol.USER_SELECT, IntoValue::into_value("text"))
}

///|
/// Matches Rust's user_select_text helper.
pub fn Style::user_select_text(self : Style) -> Style {
  self.property(
    @protocol.USER_SELECT,
    "userSelect",
    IntoValue::into_value("text"),
  )
}

///|
/// Matches Rust's visible helper.
pub fn Element::visible(self : Element) -> Element {
  self.property(@protocol.VISIBILITY, IntoValue::into_value("visible"))
}

///|
/// Matches Rust's visible helper.
pub fn Style::visible(self : Style) -> Style {
  self.property(
    @protocol.VISIBILITY,
    "visibility",
    IntoValue::into_value("visible"),
  )
}

///|
/// Matches Rust's w helper.
pub fn[T : IntoValue] Element::w(self : Element, value : T) -> Element {
  self.property(@protocol.WIDTH, IntoValue::into_value(value))
}

///|
/// Matches Rust's w helper.
pub fn[T : IntoValue] Style::w(self : Style, value : T) -> Style {
  self.property(@protocol.WIDTH, "width", IntoValue::into_value(value))
}

///|
/// Matches Rust's w_fraction helper.
pub fn Element::w_fraction(self : Element, fraction : Float) -> Element {
  self.property(
    @protocol.WIDTH,
    IntoValue::into_value(layout_fraction(fraction)),
  )
}

///|
/// Matches Rust's w_fraction helper.
pub fn Style::w_fraction(self : Style, fraction : Float) -> Style {
  self.property(
    @protocol.WIDTH,
    "width",
    IntoValue::into_value(layout_fraction(fraction)),
  )
}

///|
/// Matches Rust's w_full helper.
pub fn Element::w_full(self : Element) -> Element {
  self.property(@protocol.WIDTH, IntoValue::into_value("100%"))
}

///|
/// Matches Rust's w_full helper.
pub fn Style::w_full(self : Style) -> Style {
  self.property(@protocol.WIDTH, "width", IntoValue::into_value("100%"))
}

///|
/// Matches Rust's whitespace_normal helper.
pub fn Element::whitespace_normal(self : Element) -> Element {
  self.property(@protocol.WHITE_SPACE, IntoValue::into_value("normal"))
}

///|
/// Matches Rust's whitespace_normal helper.
pub fn Style::whitespace_normal(self : Style) -> Style {
  self.property(
    @protocol.WHITE_SPACE,
    "whiteSpace",
    IntoValue::into_value("normal"),
  )
}

///|
/// Matches Rust's whitespace_nowrap helper.
pub fn Element::whitespace_nowrap(self : Element) -> Element {
  self.property(@protocol.WHITE_SPACE, IntoValue::into_value("nowrap"))
}

///|
/// Matches Rust's whitespace_nowrap helper.
pub fn Style::whitespace_nowrap(self : Style) -> Style {
  self.property(
    @protocol.WHITE_SPACE,
    "whiteSpace",
    IntoValue::into_value("nowrap"),
  )
}
